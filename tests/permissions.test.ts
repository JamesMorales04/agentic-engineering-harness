import { describe, expect, it } from "vitest";
import { buildOpenCodeRuntimeConfig } from "../src/agents/permissions.js";
import type { AgentExecutionSelection } from "../src/agents/types.js";
describe("OpenCode permission projection", () => { it("projects Harness deny/allow decisions into runtime permission config", () => { const selection: AgentExecutionSelection = { logicalAgent: "worker", role: "implementer", domains: [], runtimeName: "opencode", runtimeAdapter: "opencode", paseoProvider: "opencode", modelAlias: "workhorse", modelId: "x/fast", modelName: "fast", transport: "direct", skills: ["backend"], mcps: [], permissions: { read: "allow", write: "allow", shell: "allow", network: "deny", delegate: "deny", gitWrite: "deny" }, args: [], runtimeCapabilities: {} }; const config = buildOpenCodeRuntimeConfig(selection) as { permission: Record<string, unknown> }; expect(config.permission.edit).toBe("allow"); expect(config.permission.websearch).toBe("deny"); expect(config.permission.task).toBe("deny"); expect(config.permission.bash).toMatchObject({ "*": "allow", "git push *": "deny" }); expect(config.permission.skill).toMatchObject({ "*": "deny", backend: "allow" }); }); });
