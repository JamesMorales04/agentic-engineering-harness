# Core Architecture v2 Conformance

**Role:** claim-to-evidence boundary map. The normative target is [Core Architecture v2](CORE_ARCHITECTURE_V2.md); current implementation gaps and next gates are in [Core Architecture v2 Status](CORE_ARCHITECTURE_V2_STATUS.md). Historical incidents and checkpoint records remain in the [Engineering Ledger](ENGINEERING_LEDGER.md).

## Verification boundary

Baseline conformance observations came from the read-only architecture audit at commit f1518caf405f44851227cf73ea37b79267b1a098 and were not rerun during that audit. S0 changed documentation only. The deterministic S1, S2, and S3 gates were accepted earlier in the campaign; the S5 context/session gate was independently reviewed and accepted on 2026-09-24 as recorded below. S6 was independently accepted on its deterministic gate on 2026-09-24; the evidence lanes below remain distinct from provider certification. Other audit observations remain at their stated evidence boundary. The Campaign Director independently accepted the S7 deterministic gate on 2026-09-24 after reviewing the schema-condition remediation and rerunning focused, typecheck, build, full non-system, and S7 deterministic system gates. REAL_PROVIDER signing remains pending.

The source and test references below identify the evidence inspected by that audit and the S1 revalidation recorded below. CONTRACT, UNIT, and INTEGRATION evidence does not imply REAL_PROVIDER, BROWSER, ADVERSARIAL, or PACKED_E2E certification. Those lanes are reported separately below. Historical test counts in the Engineering Ledger remain dated checkpoint evidence and are not current certification claims.

On 2026-09-24, the S1 callback-mutation follow-up reran deterministic focused evidence, `npx tsc --noEmit`, `npm run build`, and the full non-system Vitest suite after integration. Candidate-truth and controller-fencing prerequisites passed 2 files / 12 tests; the focused S1 set passed 15 files / 160 tests; TypeScript exited 0; build exited 0; the full non-system suite passed 149 files with 1 skipped, 768 tests with 7 skipped. The full suite emitted missing-source-map notices from its packed build-hygiene test, which passed. These are CONTRACT/UNIT and build evidence only. The launch transports were captured at deterministic adapter boundaries; no REAL_PROVIDER, container, browser, adversarial, or complete packed-journey certification is inferred.

The same-slice execution-semantics/revision remediation then reran `npx vitest run tests/operations.test.ts` — 1 file / 33 tests passed; the focused S1 set — 15 files / 165 tests passed; `npx tsc --noEmit` — exit 0; `npm run build` — exit 0; and `npx vitest run --exclude 'tests/system/**'` — 149 files passed, 1 skipped; 773 tests passed, 7 skipped. The generic future-digest preemption and absent-revision upgrade cases, including the reproduced absent-to-revision-1 patch, now reject without changing durable bytes; an owning changed-digest bind increments execution revision once and invalidates frozen policy and participant bindings. These results are deterministic contract/unit and build evidence only; no REAL_PROVIDER, browser, adversarial, container, or packed end-to-end certification is inferred.

The earlier S2 checkpoint that reported a missing production composition is superseded by the user-approved AgentTopology-to-Paseo architecture decision and the integrated implementation recorded here. On 2026-09-24, S2 deterministic verification reran `npx vitest run tests/auditIntent.test.ts tests/triage.test.ts tests/recovery.test.ts tests/candidateAssembler.test.ts tests/candidateWaveLineage.test.ts tests/candidateRepairProtection.test.ts tests/issueIntake.test.ts tests/issueDeliveryIdentity.test.ts tests/participantsStackDiscovery.test.ts tests/participantsCanonical.test.ts tests/semanticAssessment.test.ts tests/semanticAssessmentRuntime.test.ts tests/semanticAssessorTopology.test.ts tests/runRepairCandidateLifecycle.test.ts` — 14 files / 108 tests passed; `npx tsc --noEmit` — exit 0; `npm run build` — exit 0; and `npx vitest run --exclude 'tests/system/**'` — 151 files passed, 1 skipped; 788 tests passed, 7 skipped. The full suite emitted missing-source-map notices from build-hygiene checks, which passed. These deterministic fixtures verify the named public paths, typed runtime, and safeguards; they do not establish REAL_PROVIDER execution or certification.

The S2 repository-boundary and INTENT remediation was independently verified on 2026-09-24. The focused S2 command listed above passed 14 files / 123 tests; a separate stack/semantic runtime set passed 5 files / 75 tests; `npx tsc --noEmit` exited 0; `npm run build` exited 0; and the full `npx vitest run --exclude 'tests/system/**'` lane passed 151 files with 1 skipped and 803 tests with 7 skipped. The regressions bind STACK to canonical realpath and current repository content before/after capture and inference, exclude ignored `.env` and `.harness` runtime/cache evidence, reject invalid or escaping proposed project skill roots, and accept bounded HIGH-risk INTENT while retaining deterministic assurance floors. These are deterministic contract/unit/build results only. No REAL_PROVIDER, BROWSER, or ADVERSARIAL certification is claimed.

On 2026-09-24, final S3 deterministic verification after the Lead acknowledgement fencing change ran the focused authority/action command (`npx vitest run tests/security/toolActionGate.test.ts tests/humanDecision.test.ts tests/security/controllerFencing.test.ts tests/operations.test.ts tests/controlCenter.test.ts tests/architectureIntegrationContracts.test.ts tests/security/actionReconciliation.test.ts tests/deliveryFinalize.test.ts tests/operationCompletion.test.ts tests/agentPromptStructuredResultProvenance.test.ts`) — 10 files / 146 tests passed; `npx tsc -p tsconfig.json --noEmit` — exit 0; `git diff --check` — clean; `npm run build` — exit 0 (`release-1790277810343-2032089-c2f3666b`). The final full non-system command (`mapfile -t test_files < <(rg --files tests -g '*.test.ts' | rg -v '^tests/system/'); npx vitest run "${test_files[@]}" --reporter=json --outputFile=/tmp/s3-full-vitest-final.json`) completed with 318/318 suites successful, 810 tests passed, 7 pending, and zero failed. The final `npx vitest run tests/system` ran 4 files: adversarial, concurrency, and scenario-matrix suites passed 45 tests; overall 63 passed and 1 failed (64 total, no skipped). The failure is `aehHumanBlackBox.test.ts:84`: its natural-language `intent` CLI assertion received empty stdout because Paseo preflight exited with `SEMANTIC_ASSESSMENT_UNAVAILABLE`; configured model `opencode/opencode-go/MiMo-V2.6-Flash` is absent from the local catalog. Direct reproduction confirmed this environment/provider mismatch; it is not S3 evidence. The `opa` executable is unavailable, so OPA evidence is source/adapter-level only. These are CONTRACT, UNIT, build, and SYSTEM_DETERMINISTIC results; they do not establish REAL_PROVIDER, BROWSER, packed adversarial, or cancellation/takeover certification.


The preceding S3 entry is a pre-remediation checkpoint and is superseded for deterministic S3 closure by this product-choice continuation remediation. On 2026-09-24, the focused continuation command (npx vitest run tests/controlCenter.test.ts tests/humanDecision.test.ts tests/operations.test.ts tests/changeHandoff.test.ts) passed 4 files / 55 tests. The expanded authority/action/continuation command (npx vitest run tests/security/toolActionGate.test.ts tests/humanDecision.test.ts tests/security/controllerFencing.test.ts tests/operations.test.ts tests/controlCenter.test.ts tests/architectureIntegrationContracts.test.ts tests/security/actionReconciliation.test.ts tests/deliveryFinalize.test.ts tests/operationCompletion.test.ts tests/agentPromptStructuredResultProvenance.test.ts tests/changeHandoff.test.ts) passed 11 files / 153 tests. npx tsc -p tsconfig.json --noEmit exited 0; git diff --check was clean; npm run build exited 0 and produced release-1790282947173-2145162-1fdb29fd with the Control Center UI bundled. The full npx vitest run --exclude 'tests/system/**' --reporter=json --outputFile=/tmp/s3-remediation-final-vitest.json passed all 318 suites, with 814 tests passed, 7 pending, and zero failed; it emitted missing-source-map notices from the packed build-hygiene test. npx vitest run tests/system/aehAdversarialE2E.test.ts tests/system/aehConcurrencyCampaign.test.ts tests/system/aehScenarioMatrix.test.ts passed 3 files / 45 tests. These runs provide deterministic contract, unit, build, and system evidence for the S3 gate. The shared-checkout UI API mock smoke (npx tsx /tmp/s3-shared-ui-smoke.mts) passed pairing, strict DecisionRequest parsing, stale 409 handling, CSRF and submission-field checks, accepted response, and refresh behavior. The initial full-suite attempt found the ignored ui/control-center/dist output populated by the standalone UI build; that generated output was moved outside the checkout and the full suite then passed. No live OPA, browser, real-provider, packed adversarial, or provider cancellation/takeover certification is claimed.

On 2026-09-24, the S5 manifest-to-grant remediation first reproduced the old issuer defect, then passed the same authorization regression suite. The Campaign Director independently reviewed the complete S5 diff, exact manifest/envelope/ref-set checks, focused regressions, and slice gate; reran the focused context/session/provenance command (12 files / 125 tests), `npx tsc -p tsconfig.json --noEmit` (exit 0), `npm run build` (exit 0; `release-1790294205611-17-b4fe25cb`), the full non-system suite (152 files passed, 1 skipped; 835 tests passed, 7 skipped), and the three relevant deterministic system suites (3 files / 45 tests passed). The first full-suite attempt was blocked by sandbox `EPERM` errors on loopback listeners and TSX IPC; the same gate passed when rerun with the required local process/loopback permissions. `git diff --check` was clean, and the normative target had no diff. S5 is accepted on its deterministic gate. These are CONTRACT, UNIT, INTEGRATION, BUILD, and SYSTEM_DETERMINISTIC evidence only; they do not establish REAL_PROVIDER, BROWSER, OPA_PROVIDER, ADVERSARIAL, or PACKED_E2E certification.

On 2026-09-24, S6 deterministic verification ran the focused acceptance/completion/lifecycle set (`npx vitest run tests/objectiveCompletion.test.ts tests/objectiveCompletionLifecycle.test.ts tests/acceptanceOracle.test.ts tests/managedLeadAcceptance.test.ts tests/candidateAssurance.test.ts tests/runRepairCandidateLifecycle.test.ts tests/reviewLifecycle.test.ts tests/deliveryFinalize.test.ts tests/operationV2Identity.test.ts tests/operationLifecycleRegression.test.ts tests/operationSupervisionArchitecture.test.ts`) — 11 files / 124 tests passed; `npx tsc --noEmit` — exit 0; `npm run build` — exit 0 (`release-1790303504564-2721309-64c0f9cc`); full non-system Vitest — 157 files passed, 1 skipped; 931 tests passed, 7 skipped; relevant lifecycle scenario/concurrency system tests — 2 files / 17 tests passed; and `git diff --check` — clean. Lifecycle fixtures distinguish read-only audit terminalization from managed run/change success, which requires the current persisted AcceptanceOracle disposition and complete objective evidence. These are deterministic contract, unit, integration, build, and system results only; no REAL_PROVIDER, BROWSER, OPA_PROVIDER, ADVERSARIAL, or complete PACKED_E2E certification is claimed. The S6-W3 contract omits both certification disposition and identity when certification is not required; the evaluator rejects supplied values. The normative target was not changed.

The Campaign Director independently reviewed the final S6 source/test/documentation diff, reran the focused S6 set (11 files / 124 tests), `npx tsc --noEmit` (exit 0), `npm run build` (exit 0; `release-1790305046220-17-0bfdef32`), full non-system Vitest (157 files passed, 1 skipped; 931 passed, 7 skipped), and the relevant system scenario/concurrency set (2 files / 17 tests passed). The documentation-only remediation then passed `git diff --check` and confirmed no normative target diff. S6 is accepted on the deterministic gate; no REAL_PROVIDER, BROWSER, OPA_PROVIDER, ADVERSARIAL, or complete PACKED_E2E certification is claimed.

## Deterministic evidence map

This section records deterministic contracts and source/test references only. It does not promote them into provider or product-journey certification.

| Capability | Deterministic evidence referenced by the audit | Boundary or result |
| --- | --- | --- |
| Candidate identity and assembly | src/candidates/; tests/candidateIdentity.test.ts, tests/candidateWaveLineage.test.ts, tests/candidateRepairProtection.test.ts | Candidate assembly, frozen wave bases, explicit rebases, repair protection, and workspace checks are the strongest completed deterministic subsystem. Downstream impact and assurance consumers remain open. |
| WorkGraph and resource claims | src/architecture/workGraph.ts, src/agents/parallelism.ts; tests/architectureWorkGraph.test.ts, tests/parallelism.test.ts | DAG, scope, logical claims, and scheduling checks are present in the delegated path. Full operation-policy integration remains open. |
| Controller fencing and action gates | src/operations/state.ts, src/operations/controller.ts, src/security/toolActionGate.ts, src/security/actionReconciliation.ts; tests/security/controllerFencing.test.ts, tests/security/toolActionGate.test.ts, tests/security/actionReconciliation.test.ts, tests/operations.test.ts, tests/system/aehConcurrencyCampaign.test.ts | Controller-owned mutations and active terminal transitions require current epoch plus controller token; the explicit Lead acknowledgement binds current Lead actor, epoch, and operation revision. Cancellation consumes a scoped HumanDecision before takeover, rechecks current operation/candidate/policy/revision/epoch before cleanup, and blocks CANCELLED while action intents remain unresolved. Sensitive effects validate frozen policy, actor, scope, lease, candidate, and epoch; required action authorization binds exact effect digest. Final deterministic contract/unit and system evidence passed; packed adversarial takeover/effect certification remains open. |
| HumanDecision contract | src/security/humanDecision.ts, src/control-center/decision.ts, src/security/toolActionGate.ts, src/operations/state.ts, src/operations/change.ts; tests/humanDecision.test.ts, tests/controlCenter.test.ts, tests/changeHandoff.test.ts, tests/operations.test.ts, tests/security/toolActionGate.test.ts, tests/security/controllerFencing.test.ts | V2 decisions bind the current operation/candidate/execution-revision/policy/epoch identity, paired external actor, typed purpose/scope, expiry, and one-time consumption. Product choice, action authorization, and operation control are disjoint. The production Spec Manager consumer persists a typed request and continuation/checkpoint before HUMAN_REQUIRED, consumes only the exact request-bound choice, revalidates the continuation, and resumes the saved SPEC_AUTHORING stage. The consumed continuation retains its original full decision binding; the checkpoint envelope must match both the continuation and current operation across full candidate, execution revision, policy digest, and controller epoch. Revised requirements permit only one execution-revision advance and a same-epoch current-policy checkpoint rewrite; stale consumed choices are never reused: after exact old-receipt and unchanged candidate/revision checks, the controller opens a fresh current-binding HUMAN_REQUIRED request; intact unanswered requests with unchanged candidate/revision can be reissued after policy/epoch drift. A product choice grants no unrelated authority. Deterministic Control Center tests prove paired actor use and replay rejection; browser QA remains pending. |
| OPA canonical role matching | policies/core/trust-boundary.rego; tests/architectureIntegrationContracts.test.ts | Trust-boundary predicates match exact canonical `Implementer` and `Reviewer` spellings; adapter tests compare every policy role literal with the canonical role catalog. `opa` is unavailable, so no live Rego evaluation or provider certification is claimed. |
| Execution identity and result gateway | src/architecture/executionIdentity.ts, src/architecture/participantPlan.ts, src/operations/state.ts, src/workers/resultGateway.ts, src/workers/agentPrompt.ts, src/workers/runtimeSessions.ts, src/distributed/worker.ts; tests/executionIdentity.test.ts, tests/architectureWorkGraph.test.ts, tests/operations.test.ts, tests/structuredResultGateway.test.ts, tests/agentPromptStructuredResultProvenance.test.ts, tests/runtimeSessions.test.ts, tests/paseoPendingExecutionIdentity.test.ts, tests/workerExecutionAuthority.test.ts, tests/distributedExecutionIdentity.test.ts | S1 deterministically compiles versioned frozen policy, per-role ceilings, the deeply immutable expanded blueprint, and participant binding. The operation-state guard compares complete canonical policy values against a pre-callback snapshot, validates v1 on `operation.policy.bound`, allows only creation/rebinding of an unchanged policy there, and permits candidate assembly, controller takeover, or a changed previously bound semantics event to clear identity only through its owning lifecycle event. Generic add/replace/mutate/clear changes fail closed. Regressions prove same-declared-digest and in-place callback mutations fail without changing the durable record. Before any mutation callback, the guard snapshots the participant-ID set and each optional binding’s complete canonical body. It compares the union of pre-callback and post-callback participant IDs, preserving absent-versus-present binding state, so in-place mutation and participant deletion/re-keying cannot alias both sides. Binding add/replace is permitted only through the explicit validated bind event; candidate/takeover/semantics invalidation events may only clear bindings. Regressions mutate `runtime.sessionId` while retaining its stale declared digest, then separately delete/re-key the participant; each rejection preserves durable operation JSON byte-for-byte. Current result acceptance requires the full binding and checks durable candidate, execution revision, epoch, policy, output, runtime/session, and skill/context/prompt identity. OpenCode, Codex, Podman/OpenCode, Paseo, and distributed launch paths materialize or reserve an actual session before dispatch, then check the returned session against the binding. Captured valid and rejected cases are adapter-boundary evidence, not provider certification. The operation-state transition guard also snapshots executionSemanticsDigest and operationExecutionRevision presence/value before callbacks. Generic attempts to add, replace, clear, preempt the semantics digest, or synthesize an absent legacy revision are rejected; the owning event binds the first digest without advancing execution revision, treats the same digest as an identity no-op, and advances once while clearing policy and all participant bindings when replacing previously bound semantics. Candidate/takeover invalidations remain lifecycle-owned. The five new operation-state regressions assert durable-byte preservation. |
| Semantic decision surfaces | src/audit/intent.ts, src/core/triage.ts, src/entry.ts, src/operations/change.ts, src/cli.ts, src/issues/intake.ts, src/core/run.ts, src/candidates/assembler.ts, src/architecture/candidateAssurance.ts; tests/auditIntent.test.ts, tests/triage.test.ts, tests/issueIntake.test.ts, tests/recovery.test.ts, tests/candidateAssembler.test.ts, tests/candidateAssurance.test.ts | INTENT, ROUTE, STACK, ISSUE, FAILURE, and CANDIDATE_IMPACT remain MODEL assessments through the single AgentTopology-resolved AEH Semantic Assessor executed via Paseo; VALIDATION_NEED remains typed without broadening S2 scope. S4 consumes only actual post-assembly CANDIDATE_IMPACT whose deterministic candidate/base/patch bindings and digest verify against the current CandidateRevision. The deterministic compiler maps impact dimensions through frozen policy into assurance floors, actual configured Reviewer assignments, validation requirements, candidate-bound assertions, and evidence strength. Model output cannot choose identities, actions, tools, capabilities, policy, or acceptance. Explicit intent and hard assurance floors remain deterministic. S4 focused tests pass 7 files / 41 tests; full non-system verification passes 153 files / 848 tests with 7 skipped. This is deterministic fixture evidence; REAL_PROVIDER is pending. |
| Semantic Assessor topology, Paseo execution, and safety | src/semantic/assessment.ts, src/semantic/runtime.ts, src/agents/permissions.ts, src/agents/outputContracts.ts, src/workers/agentPrompt.ts; tests/semanticAssessorTopology.test.ts, tests/semanticAssessmentRuntime.test.ts, tests/semanticAssessment.test.ts | One general Semantic Assessor is resolved from AgentTopology and semantic capability policy, runs as an AEH-managed Paseo agent/session, and returns typed structured data to deterministic schema, evidence, binding, policy, receipt, provenance, digest, and cache checks. It is not a WorkGraph Participant; assessment permissions deny tools, reads from unbounded repository interfaces, writes, shell, network, delegation, acceptance, policy mutation, and capability grants. Deterministic tests verify topology, session provenance, stale/replayed evidence rejection, unknown retention, and fail-closed behavior. They do not establish REAL_PROVIDER behavior. |
| Stack discovery and detector retirement | src/participants/stack.ts, src/participants/index.ts, src/core/run.ts, src/core/git.ts, src/semantic/runtime.ts, src/architecture/validationRequirements.ts; tests/participantsStackDiscovery.test.ts, tests/participantsCanonical.test.ts, tests/semanticAssessmentRuntime.test.ts | Production stack meaning comes from the typed model judgment over bounded generic repository reads with verifiable receipts. STACK requires canonical realpath and current content digests before capture, after capture, after assessor execution, and before profile return. Its evidence paths are drawn from the same tracked/non-ignored-untracked inventory hashed by `computeWorktreeDigest`, or a stricter generic subset; ignored `.env` and `.harness` runtime/cache files are absent and cannot be cited. Deterministic code validates receipt paths and proposed project skill roots for normalization, existence, and realpath containment; these descriptive paths grant no capability. Technology-specific detector implementations, registries, APIs, fast paths, and exports were removed with no compatibility fallback. The refreshed stack suite passes 24 tests; the full focused S2 set passes 14 files / 123 tests. REAL_PROVIDER interpretation remains pending. |
| Knowledge and SkillTrust | src/knowledge/index.ts, src/agents/outputContracts.ts, src/agents/waveExecutor.ts, src/architecture/executionIdentity.ts; tests/knowledgeGate.test.ts, tests/agentPromptStructuredResultProvenance.test.ts, tests/distributedExecutionIdentity.test.ts | S1 requires grounded per-step claim/source references, retains exact accepted procedure bytes in scoped SkillManifest, projects them verbatim only to the assigned authorized participant, and binds the manifest digest at launch/result boundaries. Provider-backed Librarian research and REAL_PROVIDER evidence remain open. |
| Context Runtime v2 | src/context/, src/workers/agentPrompt.ts, src/workers/resultGateway.ts; tests/contextAuthorizationV2.test.ts, tests/contextRuntimeV2.test.ts, tests/contextEfficiency.test.ts, tests/agentPromptStructuredResultProvenance.test.ts, tests/structuredResultGateway.test.ts | S5 implements controller-fenced, read-lease-gated ContextRefAuthorizationV1 issuance; exact operation/candidate/participant-generation/blueprint/policy/epoch/session/ref/source/expiry identity; source-byte revalidation; durable candidate/session-bound retrieval receipts and budget; durable continuation sequencing and replay rejection; and result-bound ProgressiveContextManifestV1 evidence. S1 launch ContextManifest and PromptManifest digests remain immutable in ExecutionBinding. Before issuing a grant, the controller verifies the canonical launch-manifest digest against that binding, the manifest envelopeDigest against verified durable envelope provenance, and exact unique `(refId, artifactPath, sourceDigest)` set equality. Regression tests reject an extra ref in a self-consistent envelope, changed envelope digest with identical refs, and artifact-path/source-digest mismatches; exact match issues only the exact refs. The S5 deterministic gate was independently accepted after Director source/diff review and reruns: focused context/session/provenance 12 files / 125 tests; full non-system 152 files passed, 1 skipped; 835 tests passed, 7 skipped; TypeScript/build passed; relevant deterministic system suites 3 files / 45 tests. REAL_PROVIDER and live Paseo/MCP behavior remain pending. |
| Paseo session binding | src/paseo/sessionBinding.ts, src/paseo/launchSpec.ts, src/paseo/sdk.ts, src/paseo/runtimeCore.ts, src/workers/agentPrompt.ts; tests/paseo/sessionBinding.test.ts, tests/agentPromptStructuredResultProvenance.test.ts, tests/paseoPendingExecutionIdentity.test.ts, tests/paseoInitialTurnBarrier.test.ts | Production prompt preparation persists strict canonical PaseoSessionBindingV1 identity over operation/revision, participant/generation, candidate, blueprint, policy, context/prompt, controller epoch, and actual provider session. Reuse requires ACTIVE full equality; session drift rotates to a new actual session or rejects reuse, and the StructuredResult retains the returned actual session. Focused binding and launch/result tests pass; full non-system lane passes 320 files / 830 tests with 7 pending. Deterministic fixtures do not certify REAL_PROVIDER session behavior. |
| CandidateImpact and assurance | src/candidates/assembler.ts, src/candidates/wave.ts, src/candidates/repair.ts, src/core/run.ts, src/architecture/candidateAssurance.ts, src/architecture/acceptanceOracle.ts; tests/candidateAssembler.test.ts, tests/candidateAssurance.test.ts, tests/candidateWaveLineage.test.ts, tests/candidateRepairProtection.test.ts, tests/runRepairCandidateLifecycle.test.ts, tests/acceptanceOracle.test.ts | S4 consumes exact post-assembly impact and compiles current assurance assertions; S6 consumes these unchanged candidate/impact/policy/evidence-strength bindings and rejects stale evidence. Deterministic S4/S6 coverage is present; MODEL interpretation and live provider/session behavior remain separate. |
| Validation | src/core/verify.ts, src/core/run.ts, src/architecture/validationRequirements.ts, src/architecture/candidateAssurance.ts, src/validators/external.ts; tests/validationProviders.test.ts, tests/candidateAssurance.test.ts, tests/candidateAssembler.test.ts, tests/runRepairCandidateLifecycle.test.ts | Typed impact requirements resolve under frozen policy and are checked against resolver digest, exact requirement/action kind, scope, and evidence needs. The approved project/configured action is executed; missing, disallowed, unsupported, or non-PASS required evidence fails closed and records candidate/impact/policy identity. Playwright/OpenGrep/Trivy provider names use matching external adapters; unknown specialized providers cannot fall through to generic unit tests. Validation PASS is not product acceptance. Focused S4: 7 files / 41 tests; full non-system: 153 files passed, 1 skipped; 848 passed, 7 skipped. Provider/browser lanes remain pending. |
| Review and convergence | src/agents/reviewLifecycle.ts, src/core/run.ts; tests/candidateAssurance.test.ts, tests/reviewLifecycle.test.ts, tests/reviewRepairCandidateLifecycle.test.ts, tests/runRepairCandidateLifecycle.test.ts | S4 assignments are actual configured canonical read-only Reviewer invocations bound to candidate, impact, policy, dimensions, and exact returned session identity. S6 requires a matching current participant ExecutionBinding and persisted result artifact; reviewer agreement remains evidence and never acceptance. Repair/rollback recompiles for the resulting candidate. Focused S6: 11 files / 124 tests; full non-system: 157 files passed, 1 skipped; 931 passed, 7 skipped. REAL_PROVIDER pending. |
| Acceptance | src/architecture/candidateAssurance.ts, src/architecture/acceptanceOracle.ts, src/architecture/objectiveCompletion.ts, src/agents/managedLeadAcceptance.ts, src/core/run.ts, src/operations/state.ts; tests/acceptanceOracle.test.ts, tests/managedLeadAcceptance.test.ts, tests/objectiveCompletion.test.ts, tests/objectiveCompletionLifecycle.test.ts, tests/runRepairCandidateLifecycle.test.ts, tests/deliveryFinalize.test.ts | S4 assertions remain candidate/impact/policy-bound; S6 resolves VerificationRequirements, constructs current provenance/freshness/execution-bound EvidenceBundles, checks validation strength and actual independent Reviewer evidence, obtains managed Lead semantic evidence when frozen policy requires it, and persists controller-evaluated ACCEPTED/REJECTED disposition. Terminalization reloads the current artifact, recomputes objective DoD, and matches objective assertion coverage to persisted oracle coverage. Validation PASS, reviewer agreement, completion claims, and Lead opinion alone cannot accept. CertificationCore remains a distinct evidence producer. Deterministic acceptance/completion tests pass; live managed provider and packed journeys remain pending. |
| Delivery and reconciliation | src/delivery/finalize.ts, src/architecture/acceptanceOracle.ts, src/operations/state.ts, src/security/gatedAction.ts, src/security/toolActionGate.ts, src/security/actionReconciliation.ts, src/provenance/generate.ts; tests/deliveryFinalize.test.ts, tests/objectiveCompletionLifecycle.test.ts, tests/security/toolActionGate.test.ts, tests/security/actionReconciliation.test.ts, tests/system/aehScenarioMatrix.test.ts, tests/system/aehConcurrencyCampaign.test.ts, tests/system/aehAdversarialE2E.test.ts | Delivery retains S6 AcceptanceOracle-before-effects ordering, then verifies current workspace/candidate identity and policy-required S7 provenance before commit, push, or PR requests. A new delivery regression proves missing strict provenance blocks before requests or commit. S8 controller-owned delivery/reconciliation remains outside S7. The focused S7 delivery set passes; real external effects and complete packed reconciliation are not certified. |
| Control Center | src/control-center/, src/entry.ts, ui/control-center/src/api.ts, ui/control-center/src/App.tsx; tests/controlCenter.test.ts, tests/controlCenterApiContracts.test.ts, tests/humanDecision.test.ts, tests/operations.test.ts | The authenticated UI projects the current bounded DecisionRequest, displays evidence/options/consequences and the saved resume target, and submits only operation/request/choice identifiers plus optional reason. The paired server derives actor and current operation/candidate/policy/revision/epoch and rejects stale, expired, malformed, mismatched, or replayed choices. UI typecheck/build and deterministic API boundary tests pass; browser QA remains pending. |
| Supply chain | src/provenance/generate.ts, src/build/identity.ts, src/core/config.ts, src/core/types.ts, schemas/project.schema.json, src/delivery/finalize.ts, src/cli.ts, src/core/doctor.ts; tests/provenance.test.ts, tests/provenanceS7.test.ts, tests/projectSchemaS7.test.ts, tests/buildHygiene.test.ts, tests/deliveryFinalize.test.ts, tests/system/aehScenarioMatrix.test.ts, tests/system/aehAdversarialE2E.test.ts | Strict version-2 evidence binds the exact CandidateRevision, configured packed-artifact path/digest, running BuildIdentity, supply-chain policy digest, expected builder, CycloneDX SBOM digest, in-toto subject, SLSA predicate, and policy-required Cosign verification. Missing, malformed, stale, tampered, wrong-artifact/candidate/build/policy/builder evidence blocks; S6 AcceptanceOracle remains required before the pre-effect S7 gate. Both provenance schema conditions now require the parent object and nested required flag before matching const: true; config loading verifies non-strict and strict policy behavior. Deterministic fake signing proves only the contract and is not REAL_PROVIDER certification. The Campaign Director accepted the S7 deterministic gate on 2026-09-24. |
| Certification contracts | src/certification/; tests/certification.test.ts, tests/certificationMatrix.test.ts | Required-evidence enforcement and capability matrix contracts have deterministic references. They do not establish real-provider certification. |

The Campaign Director independently accepted S4 on 2026-09-24 after reviewing the source diff and affected evidence records, then rerunning the focused candidate-assurance set (7 files / 41 tests), `npm run typecheck`, `npm run build` (`release-1790299600802-17-85b48e9b`), and the full non-system Vitest lane (153 files passed, 1 skipped; 848 tests passed, 7 skipped). These deterministic results do not certify REAL_PROVIDER, BROWSER, OPA_PROVIDER, ADVERSARIAL, or PACKED_E2E behavior.

## REAL_PROVIDER lane

**Status from the audit:** not established for the complete claimed capability matrix.

A real-provider claim requires a freshly packed candidate, provider startup and execution receipts, current candidate/build identity, deterministic oracle checks, and required independent review. Deterministic fixtures and contract tests cannot certify real providers.

The audit identifies incomplete or unproven real-provider journeys for semantic assessment, Librarian research, Paseo, Podman/OCI isolation, integration environments, browser validation, delivery/reconciliation, and the full capability matrix. S2 deterministic Paseo fakes do not change that status. The Serena smoke recorded in the historical ledger proves only the tested Serena MCP broker path; it does not certify unused provider lease/release lifecycle or model execution.

## BROWSER lane

**Status from the audit:** pending.

No browser provider/candidate journey certification is established. The external validator adapter and requirement mapping are deterministic/contract evidence only. Browser and visual claims require candidate-bound traces/screenshots and a packed UI journey, including the relevant Control Center flow.

## ADVERSARIAL lane

**Status from the audit:** complete self-hosting adversarial/fault-injection evidence is not established.

Deterministic negative tests and fail-closed contract checks remain in the deterministic evidence map. They are not substituted for adversarial or fault-injection campaigns against the packed candidate, authority boundary, cancellation/takeover behavior, isolation, and external-effect reconciliation.

## PACKED_E2E lane

**Status from the audit:** partial campaign/bootstrap evidence exists; complete packed-candidate product journeys are not established.

Some historical campaign evidence exercised packed consumer behavior, but the all-packed campaign description was overbroad: some campaign tests import src/** directly. A packed bootstrap or a direct-source campaign is not proof that every product journey ran from a freshly packed candidate. Full packed journeys remain required for claimed capabilities, including human interaction, candidate mutation/repair, acceptance, delivery, provider lifecycle, and disposable self-modification.

## Reconciled claims

- **Authority and HumanDecision:** deterministic production consumers now cover exact action authorization, cancellation, and product-choice continuation. Requests and decisions bind current operation, candidate, execution revision, policy, actor, purpose/scope, and epoch; product choice cannot satisfy action authorization or operation control. Live OPA, browser, provider, and packed adversarial certification remain separate pending lanes.
- **Cancellation:** deterministic focused and system evidence now proves current controller ownership is claimed before cleanup, current operation/candidate/policy/revision/epoch fences are rechecked through cleanup, and unresolved action intents prevent terminal CANCELLED. Packed provider/fault-injection certification remains pending.
- **Validation, review, and acceptance:** these are separate gates. S4 runs or blocks impact-derived validation and creates actual independent Reviewer assignments with candidate-bound session evidence. S6 consumes current validated checks and exact Reviewer session provenance as evidence, requests bound managed Lead semantic evidence when frozen policy requires it, and has the controller persist the AcceptanceOracle disposition. Validation PASS, review agreement, participant completion, completion claims, and Lead opinion alone remain insufficient. Delivery and terminal success require the current oracle and objective gate.
- **CandidateImpact:** actual post-assembly impact is consumed by a deterministic assurance compiler against exact candidate and frozen policy identity. `requiresIndependentReview` causes an actual configured Reviewer assignment when required; it remains input evidence and grants no authority. S6 consumes the compiled candidate/impact/policy-bound assertions and evidence-strength floor without changing the S4 interface.
- **Repairer:** candidate-assembly repair support exists, so the public path is not accurately described as having no Repairer mutation support. S4 verifies the runTask validation-repair path and review quality-repair/rollback recompile impact-driven assurance against the resulting current candidate. S6 consumes only the resulting current candidate evidence; broader recovery/provider journeys remain pending.
- **StructuredResult provenance:** candidate/generation/epoch checks exist, and blueprint/policy checks are optional in some paths. Production launchers omit required full digests; do not describe provenance as wholly absent or complete.
- **ExecutionCatalog:** the catalog exists and is used. Its policy/tool authorization fields are not fully enforced; do not describe the catalog itself as absent.
- **Packed campaign:** some campaign tests import source directly. Do not label all campaign tests or all product journeys packed.
- **Serena:** the historical smoke covers the tested Serena broker path only, not provider-lease lifecycle or real-model certification.
- **Dated validation counts:** retain them only as dated checkpoint evidence in the ledger. They are not rerun results or current certification.
- **Delivery description:** the prior audit note about the PR description predates the S6 gate. Current delivery effects are accepted only after the persisted current AcceptanceOracle disposition; required managed Lead evidence precedes that disposition.

## S7 deterministic evidence — 2026-09-24

The focused provenance/build/delivery command (`npx vitest run tests/provenance.test.ts tests/provenanceS7.test.ts tests/architectureIntegrationContracts.test.ts tests/deliveryFinalize.test.ts --reporter=dot`) passed 4 files / 49 tests. `npx tsc -p tsconfig.json --noEmit` exited 0; `npm run build` exited 0 and produced `release-1790308464438-2905085-da2062aa`; JSON schema parsing and `git diff --check` passed. The final full non-system suite passed 158 files with 1 skipped and 965 tests with 7 skipped. The build-hygiene pack test emitted missing-source-map notices but passed.

The three relevant deterministic system suites ran 3 files: scenario and concurrency passed, while the adversarial suite had 2 failures (43 passed / 2 failed overall). Both failures are terminal-operation fixtures rejected with `OBJECTIVE_COMPLETION_REQUIRED` because they lack the objective-completion/current AcceptanceOracle evidence now required by S6. The S7-specific missing-manifest system case was isolated and passed (1 passed; 27 other tests skipped). No S6 lifecycle fix was made in S7. REAL_PROVIDER signing was not run because the `cosign` CLI is unavailable in this environment; fake Cosign/PATH-shim fixtures are deterministic contract evidence only. At the initial S7 close, Campaign Director acceptance remained pending; the schema remediation and subsequent independent acceptance are recorded below.

## S7 schema-guard remediation — 2026-09-24

The independent-review finding was reproduced before the schema edit in the Flash worker's separate Paseo worktree. The command npx vitest run tests/projectSchemaS7.test.ts --reporter=dot reported 1 failed file, 6 structural guard tests failed, and 15 tests passed. The failures identified absent parent required entries for provenance.required, sbom, signing, and verification plus absent nested required guards for their boolean flags. The 15 passing tests already confirmed both then constraints and the existing loadProjectConfig behavior. The worker also confirmed the 21-test suite passes against a throwaway schema copy with the full guards; the primary schema remained unchanged until the red run was reported.

The repair adds those guards to both allOf conditions and changes no runtime config contract. No AJV or other JSON Schema validator is installed or resolvable, so JSON Schema coverage is explicitly structural; the test suite uses the existing Zod loader to accept the shipped non-strict template, minimal/false-flag configurations, and valid strict configuration, and reject missing artifact/key cases. No validator dependency was added. Post-fix, npx vitest run tests/projectSchemaS7.test.ts --reporter=dot passed 1 file / 21 tests. The focused command npx vitest run tests/provenance.test.ts tests/provenanceS7.test.ts tests/projectSchemaS7.test.ts tests/architectureIntegrationContracts.test.ts tests/deliveryFinalize.test.ts --reporter=dot passed 5 files / 70 tests; npx tsc -p tsconfig.json --noEmit exited 0; npm run build exited 0 (release-1790309899170-2946073-c0075cc5); JSON schema parsing passed. The S7 system scenario matrix passed 8 tests and the adversarial missing-manifest case passed 1 test (27 skipped).

The previously recorded broader adversarial run's two terminal-operation fixture failures remain the known S6 objective-completion/current-AcceptanceOracle fixture issue. This remediation did not rerun or change those fixtures. The isolated S7 system checks passed. The Campaign Director independently reran the focused S7 set (5 files / 70 tests), TypeScript noEmit, production build (release-1790310426337-17-0e3b4825), full non-system Vitest (159 files passed, 1 skipped; 986 passed, 7 skipped), scenario matrix (8 passed), adversarial missing-manifest case (1 passed, 27 skipped), JSON schema parsing, and diff checks. REAL_PROVIDER signing remains unrun because cosign is unavailable; deterministic fake signing remains fixture evidence only. The Campaign Director accepted the S7 deterministic gate on 2026-09-24; broader external certification lanes remain pending.

## Certification rule

CertificationCore evaluates frozen policy against candidate/build-bound receipts and required evidence. It does not infer provider success from deterministic tests, browser success from adapter contracts, adversarial robustness from ordinary negative tests, or packed-candidate behavior from direct-source test imports. Each lane reports its own status and evidence.


The Campaign Director’s S3 source review identified two stale-binding recovery gaps in consumed-choice and checkpoint loading. On 2026-09-24, the Luna-owned remediation added a durable `selectedDecisionBinding`, exact full-binding comparisons in `loadConsumedProductChoice` and `loadOperationProductChoiceCheckpoint`, checkpoint identity rewrites only during the authorized same-epoch requirement/policy lifecycle, and fail-closed consumed-choice recovery. Regressions cover decision and current-operation execution revision, policy digest, and controller epoch drift; checkpoint envelope tampering with recomputed content digests; safe reissue of an intact unanswered request after takeover; and fresh HUMAN_REQUIRED reconfirmation after consumed-choice takeover. The bounded interruption after execution-semantics persistence is recovered by the controller’s existing bootstrap: it binds current policy before continuation loading, then verifies current continuation/checkpoint identity. If takeover changed the epoch, it retains the old decision binding only to verify the consumed receipt and opens a new current-binding HUMAN_REQUIRED request; it never rebinds the old choice. Final verification: focused S3 authority/action command passed 12 files / 167 tests; `npx tsc -p tsconfig.json --noEmit` exited 0; `npm run build` exited 0 (`release-1790285875947-2230303-2736c95d`); full non-system Vitest passed 318/318 suites, 819 tests passed, 7 pending, zero failed; the three S3 deterministic system suites passed 45 tests. `git diff --check` was clean. These are deterministic contract/unit/build/system results only; live OPA, browser, real-provider, and packed adversarial/provider takeover certification remain pending. The full-suite packed build-hygiene test emitted missing-sourcemap notices but passed. The Campaign Director independently accepted the S3 deterministic gate after reviewing the lifecycle implementation and rerunning the focused authority set (12 files / 157 tests), TypeScript check, and production build; OPA, browser, provider, and packed adversarial evidence remains unclaimed.
