# AEH Core Architecture v2 Engineering Ledger

This ledger records evidence-backed findings for the external Codex-led architecture campaign. It is not an AEH OperationRecord and is intentionally maintained outside the candidate workflow.

Findings, commands, counts, and status labels below are historical at their recorded checkpoints. They do not claim current certification unless a separately dated verification says so. For the f1518caf405f44851227cf73ea37b79267b1a098 read-only audit snapshot, consult the status and conformance documents; that audit did not rerun the evidence.

## Findings

| ID | Severity | Component | Symptom | Root cause | Files | Regression | Fix | Deterministic verification | Real E2E verification | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AEH-V2-0001 | P1 | Build/runtime isolation | A detached operation launched from the development checkout temporarily made `dist/main.js` unavailable; a concurrent CLI status invocation failed with `MODULE_NOT_FOUND`. The accidental audit later failed when its Paseo transport disconnected. | The self-hosted detached operation and its build lifecycle shared the development checkout's mutable `dist` output. `npm run build` removed `dist` before recompiling, so concurrent control-plane invocations had no stable executable boundary. | `package.json`, `scripts/build.mjs`, `tests/buildHygiene.test.ts` | `tests/buildHygiene.test.ts` runs a real build while checking the entrypoint, CLI probes and retained release set. | `npm run build` compiles in a unique staging directory, publishes a complete immutable release behind an atomic `dist/current` pointer and atomic launcher, serializes publishers with a lock, and retains only the current plus two exact release directories. | `npm run typecheck && npm run build`; `tests/buildHygiene.test.ts` — 3 passed; packed dry-run — 2,146 files, 0.94 MB compressed, 9.60 MB unpacked. | Not run; prohibited on the development checkout. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0002 | P1 | Operation cancellation/lifecycle | Cancelling a detached operation with a controller PID and no registered participants exceeded the 10-second regression timeout. | Cleanup attempted a live Paseo agent discovery even when the temporary operation root had no project configuration and no evidence that Paseo was configured; the discovery command could consume its full 30-second timeout. | `src/operations/controller.ts`, `tests/operations.test.ts` | Existing detached-descendant cancellation regression now completes within its bounded test timeout. | Paseo compatibility discovery is gated on a configured Paseo orchestration provider; process-group/descendant cleanup remains unconditional. | `npx vitest run tests/operations.test.ts` — 16 passed. | Not required for this deterministic lifecycle fix. | FIXED_AND_RECERTIFIED |
| AEH-V2-0003 | P0 | Architecture contract/lifecycle boundary | The pre-v2 surface represented implementation as a binary Quick/SPEC decision and had no shared candidate, receipt, authority, runtime ownership, or project identity contract. | Architecture intent, implementation route, assurance, candidate identity, runtime terminal evidence, capability leases, context addressing, and project registration were spread across incompatible legacy shapes or absent. | `src/architecture/`, `src/agents/routingV2.ts`, `src/operations/v2Contracts.ts`, `src/operations/state.ts`, `src/security/`, `src/runtime/`, `src/context/runtimeV2.ts`, `src/projects/`, `src/control-center/` | New deterministic contract, lifecycle, routing, runtime, authority, context, registry, Control Center and certification-matrix tests cover the v2 foundation. | Added explicit four-way implementation routing plus independent assurance; candidate-bound operation receipts and success terminal gate; scoped runtime/Serena ownership; external human decision ledger; registry-backed Home and loopback token/CSRF Control Center. | `npm run typecheck`; focused v2 suites; non-system Vitest — 133 files passed, 551 tests passed, 7 skipped. | Not run against the development checkout; later candidate certification remains isolated. | FOUNDATION_IMPLEMENTED_AND_DETERMINISTICALLY_VERIFIED |
| AEH-V2-0004 | P1 | Packaged candidate boundary | Source-only tests can miss missing packaged assets or entrypoint wiring. | The architecture campaign requires the candidate artifact to be exercised from a clean consumer boundary, separate from the development checkout. | `package.json`, `dist/`, `src/entry.ts`, `src/control-center/`, `src/projects/` | Fresh temporary packed consumer was extracted and run independently. | No source checkout workflow was used; candidate was packed, extracted, linked only to the existing dependency tree, and exercised through packaged `init`, deterministic `start`, `home --once`, `control-center --once`, project registration/listing and two-project isolation. | Latest packed candidate `0.8.4` smoke passed: deterministic user turn completed; two registered projects received distinct identities; Home and Control Center bound loopback ports; temporary fixtures were cleaned and no child AEH/Paseo processes remained. | Deterministic packed-consumer smoke PASS. Real external-model/provider lanes remain explicitly unclaimed. | PACKAGED_DETERMINISTIC_SMOKE_PASS |
| AEH-V2-0005 | P0 | Candidate terminal truth | A successful operation could be accepted without candidate-bound receipts when the controller had no participant evidence; detached startup also did not bind the candidate revision before terminal evaluation. | The v2 terminal gate was present but optional when no candidate was supplied, and legacy operation bootstrap records were not normalized to a candidate identity. | `src/operations/state.ts`, `src/operations/controller.ts`, `src/workers/agentPrompt.ts`, `tests/operationV2Identity.test.ts`, `tests/operationLifecycleRegression.test.ts` | Candidate identity, revision binding, receipt provenance and success-terminal rejection are covered by deterministic lifecycle tests. | Operation records normalize a compatibility candidate, detached controllers bind the current revision, and successful terminal transitions require matching candidate-bound receipts/artifacts. | `tests/operationV2Identity.test.ts`, `tests/operationLifecycleRegression.test.ts` — PASS. | Real model/provider evidence not claimed. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0006 | P1 | Routing / feature handoff | Canonical route and assurance metadata could be treated as advisory while compatibility `mode` selected execution; delegated work had no compact normative handoff, and durable escalation could leave formal contracts carrying the old route. | Production selection, review, planning and evidence paths still keyed important decisions to Quick/SPEC compatibility or ignored route metadata. | `src/agents/routing.ts`, `src/core/run.ts`, `src/operations/change.ts`, `src/architecture/featureCapsule.ts`, `tests/routing.test.ts`, `tests/architectureV2Contracts.test.ts` | Route/assurance selection, delegated capsule validation and route-aware review/acceptance tests pass. | Canonical four-way routes and assurance drive selection and gates; delegated changes persist a validated FeatureCapsule; durable escalation rewrites route evidence and formal contract metadata. Removed the obsolete Quick/mode surface from active source, templates and tests. | Non-system Vitest — 133 files passed, 551 tests passed, 7 skipped. | Real delegated/formal model journey remains pending in an isolated candidate fixture. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0007 | P1 | Serena runtime isolation | Per-agent Serena launch paths could create duplicated processes and a writer projection could expose raw editing capability outside an explicit lease. | Production launch/configuration did not use a shared headless broker; projection and lease enforcement were not centralized. | `src/providers/serenaProxy.ts`, `src/runtime/serenaPool.ts`, `src/agents/permissions.ts`, `src/paseo/launchSpec.ts`, `tests/serenaPool.test.ts` | Pool identity, reader/writer conflict and projection tests pass. | A shared Unix-socket Serena broker starts one headless server, multiplexes readers, enforces one writer lease, hides edit tools from readers, and returns only the projected MCP surface. | Actual Serena 1.6.1 pooled MCP smoke passed with two clients, nine projected tools each and edit tools hidden from the reader; no child processes remained. | Supported-provider model execution is not claimed; Paseo executable is unavailable in this environment. | FIXED_AND_ISOLATED_PROVIDER_SMOKE_PASS |
| AEH-V2-0008 | P1 | Runtime / Control Center authority | Runtime state writes from independent owners could overwrite each other; Control Center decisions lacked a project/candidate-bound callback; normal start did not materialize the local authority surface. | Runtime persistence was last-writer-wins and the Control Center UI/server boundary had no durable decision integration. | `src/runtime/managed.ts`, `src/entry.ts`, `src/control-center/decision.ts`, `src/control-center/server.ts`, `tests/runtimeSupervisorV2.test.ts`, `tests/controlCenter.test.ts` | Independent runtime-owner merge and decision callback tests pass; packed loopback startup smoke passes. | Runtime snapshots merge under a file lock, normal non-deterministic start launches a tokenized loopback Control Center, and decision callbacks validate operation/candidate scope before writing the human decision ledger. | Non-system Vitest — PASS; packed consumer and direct loopback Control Center smoke — PASS. Browser automation was unavailable, so browser visual QA is not claimed. | Paseo/human-facing journey remains pending in an isolated candidate fixture. | FIXED_AND_RECERTIFIED (deterministic + loopback) |
| AEH-V2-0009 | P1 | Certification evidence boundary | Capability rows with `requiredEvidence` could report PASS without enforcing that evidence, allowing configuration-only checks to masquerade as model/provider certification. | Certification evaluated checks and overall status but did not resolve required evidence tokens against oracle evidence and passing checks. | `src/certification/core.ts`, `tests/certification.test.ts`, `tests/certificationMatrix.test.ts` | Missing required evidence now yields `INSUFFICIENT_EVIDENCE`/non-PASS capability results. | Certification resolves required evidence explicitly and separates deterministic contract PASS from model/provider PASS. | Certification suites and non-system Vitest — PASS. | Real model/provider lanes remain unclaimed until isolated candidate certification. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0010 | P0 | Participant terminal authority | A syntactically valid receipt from an unregistered participant could be recorded and contribute to terminal success. | Receipt recording validated the receipt/candidate shape but did not perform the registration check inside the operation-state lock. | `src/operations/state.ts`, `tests/operationV2Identity.test.ts` | Unknown participant receipt rejection is covered by the operation identity suite. | Receipt recording now rejects participants absent from the current operation while holding the state lock, so a concurrent registration race cannot bypass the gate. | Non-system Vitest — 133 files passed, 551 tests passed, 7 skipped. | Real participant runtime evidence remains pending in isolated candidate certification. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0011 | P0 | Context reference authority | A caller could self-authorize a ContextRef by supplying operation/project/candidate fields without a controller-issued grant; authorization could also issue refs from a self-consistent durable envelope that was not represented in the bound launch manifest. | ContextRef creation treated caller-provided scope as sufficient authorization and did not validate a controller-issued durable receipt. Grant issuance reread the durable ContextEnvelope without checking its verified digest or exact `(refId, artifactPath, sourceDigest)` set against the frozen S1 ContextManifest. | `src/context/runtimeV2.ts`, `src/context/authorizationV2.ts`, `tests/contextRuntimeV2.test.ts`, `tests/contextAuthorizationV2.test.ts` | Self-authorization, stale/forged scope, expiry, source drift, wrong participant/session, replay, and durable budget checks pass. Controller issue requires current S3 ownership plus a current participant read lease and rejects launch/envelope digest or exact `(refId, artifactPath, sourceDigest)` set mismatches. | Strict ContextRef authorization binds operation/revision, project, candidate, participant/generation, blueprint, policy, controller epoch, actual session, exact permitted fragment/source identity, expiry, budget, and final S1 ExecutionBinding receipt. Before constructing grants, the issuer checks the immutable launch ContextManifest digest against that binding, its envelopeDigest against verified envelope provenance, and exact unique addressable-ref tuple equality; source bytes are revalidated. Digest integrity alone is not treated as proof of controller origin. | S5 remediation: authorization red/green 1 file (4 failed / 4 passed before fix; 8 passed after); focused context/session/provenance 12 files / 125 tests; full non-system 152 files, 835 passed / 7 skipped; TypeScript/build passed; relevant deterministic system suites 3 files / 45 tests passed. Campaign Director independently reran the focused, full non-system, build/type, and relevant system gates and accepted S5 deterministic closure. | Provider/model context journey remains pending; no REAL_PROVIDER claim. | FIXED_AND_RECERTIFIED (S5 deterministic); REAL_PROVIDER pending |
| AEH-V2-0012 | P1 | Candidate identity propagation | A managed validation report could be persisted without the candidate revision, leaving review, quality, evidence and delivery artifacts unable to prove they refer to the same source. | Candidate identity existed in operation state but was not projected into ValidationReport or downstream artifacts. | `src/core/verify.ts`, `src/core/types.ts`, `src/agents/reviewLifecycle.ts`, `src/agents/qualityConvergence.ts`, `src/evidence/graph.ts`, `src/delivery/finalize.ts`, `tests/candidateIdentity.test.ts` | Managed verification now carries the current candidate revision; quality state, evidence graph and delivery result carry its digest/identity. Remediation rejects candidate drift. | Non-system Vitest — 133 files passed, 551 tests passed, 7 skipped. | Real candidate mutation/review/delivery journey remains pending in isolated candidate certification. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0013 | P0 | Execution authority enforcement | Direct, Podman, Paseo and distributed worker adapters could launch without a capability lease tied to a managed operation and candidate. | Authority preparation existed only on selected prompt paths and was optional for concrete worker executors. | `src/security/executionLease.ts`, `src/workers/direct.ts`, `src/workers/podman.ts`, `src/workers/paseo.ts`, `src/distributed/worker.ts`, `tests/workerExecutionAuthority.test.ts` | Unmanaged launches are rejected before spawn; managed direct launches propagate participant, candidate digest and capability leases; distributed jobs require and revalidate the authority envelope. | All concrete worker adapters now fail closed without managed candidate authority and persist authority on WorkerSession. | `npx tsc --noEmit`; non-system Vitest — 133 files passed, 551 tests passed, 7 skipped; focused authority suite — 4 passed. | Real provider/model launch remains pending in isolated candidate certification. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0014 | P1 | Participant plan runtime binding | The compiler produced a deterministic ParticipantPlan/ExecutionBlueprint, but wave execution still launched work units without consuming compiled participant identity or current candidate revision. | Blueprint metadata was recorded for telemetry only; executor selection remained detached from assignment identities. | `src/architecture/participantPlan.ts`, `src/agents/waveExecutor.ts`, `src/distributed/worker.ts`, `tests/architectureWorkGraph.test.ts` | Each wave resolves work units through the compiled assignment map; launch authority receives the compiled participant ID and blueprint records the managed candidate revision. | Runtime roles/tool packs remain compiled from canonical role profiles; model output cannot choose concrete agents or widen scope. | `npx tsc --noEmit`; non-system Vitest — 133 files passed, 551 tests passed, 7 skipped. | Real multi-worker participant journey remains pending in isolated candidate certification. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0015 | P1 | Participant architecture / knowledge / stack | Runtime participant identity, skills, tools, stack detection and knowledge sufficiency were previously distributed across ad hoc topology and provider paths. | There was no deterministic compiler boundary for canonical roles, capability packs, stack detector precedence, or knowledge-gap status. | `src/participants/`, `src/knowledge/index.ts`, `tests/participantsCanonical.test.ts`, `tests/participantsStackDiscovery.test.ts`, `tests/knowledgeGate.test.ts` | Canonical role/skill/tool catalogs, registry-backed stack detection and explicit KnowledgeGate statuses are covered by deterministic tests. | Added participant compiler foundations, skill/tool registries, StackDetectorRegistry and KnowledgeGate/Librarian status model; model output cannot mint runtime roles or skills. | Non-system Vitest — 133 files passed, 551 tests passed, 7 skipped. | Real external knowledge discovery remains pending in isolated candidate certification. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0016 | P1 | Candidate assembly / impact | Worker patches could be applied without one controller-owned change-set and without candidate-bound impact interpretation. | Patch application and candidate revision advancement were centralized at the wave boundary, but impact categories and review dimensions were still inferred from path heuristics. | `src/candidates/assembler.ts`, `src/agents/waveExecutor.ts`, `tests/candidateAssembler.test.ts` | Stale operation/participant/candidate bindings, patch digest drift and scope escapes are rejected; bound semantic impact claims are checked against every observed changed path; absent semantics fail closed to independent review. | Added ChangeSet, CandidateAssembler and candidate-bound wave assembly. Candidate impact now accepts only a typed `MODEL`/`HYBRID` assessment bound to changed-file evidence; no assessment yields `BLOCKED` impact and `requiresIndependentReview=true`. | Focused candidate suite — 3 passed; full non-system Vitest and `npx tsc --noEmit` are rerun after the current campaign slice. | Real multi-participant mutation/review journey remains pending in isolated candidate certification. | FIXED_AND_RECERTIFIED (deterministic boundary; provider integration pending) |
| AEH-V2-0017 | P1 | Control Center API / Paseo boundary | The UI transport exposed broad untyped data and had no bounded AEH-owned Paseo integration boundary. | Resource projections, event IDs, knowledge statuses and Paseo interactions were coupled to inline server details or left implicit. | `src/control-center/contracts.ts`, `src/control-center/server.ts`, `src/paseo/gateway.ts`, `ui/control-center/`, `tests/controlCenterApiContracts.test.ts`, `tests/paseoGateway.test.ts` | Authenticated resource projections, operation detail, event history, degraded gateway state and typed knowledge statuses pass deterministic tests. | Added versioned Control Center projections, one separately buildable React Home/Project frontend, and bounded PaseoGateway methods for lead conversation, participant timelines and capability preflight. | Non-system Vitest — 133 files passed, 551 tests passed, 7 skipped; `npx tsc --noEmit`; isolated temporary-directory frontend `npm install` + `npm run build` PASS. | Browser/UI visual QA and real Paseo journeys remain pending in isolated candidate certification. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0018 | P1 | Decision mechanism / semantic reasoning boundary | Heuristic routing, stack and issue interpretation can grow into an implicit expert system, while small semantic judgments risk being materialized as unnecessary participants. | Core-v2 had no shared typed contract for bounded model assessments, profile capability resolution, evidence-bound caching or non-authoritative provenance. | `AGENTS.md`, `src/semantic/assessment.ts`, `src/agents/routingV2.ts`, `src/core/triage.ts`, `src/issues/intake.ts`, `src/core/errors.ts`, `tests/semanticAssessment.test.ts`, `tests/issueIntake.test.ts`, `tests/implementationRoutingV2.test.ts`, `tests/triage.test.ts`, `tests/changeRouting.test.ts`, `docs/CORE_ARCHITECTURE_V2_CONFORMANCE.md` | Cheapest certified profile selection, evidence-reference validation, cache invalidation, escalation and authority-denial tests pass; route and assurance remain independently gated; issue normalization blocks without semantic planning and preserves raw evidence. | Added `SemanticAssessmentServiceV1` as an injected, bounded, read-only capability with typed assessment requirements, capability profiles, deterministic profile resolution, cache/provenance digests and telemetry hooks. Route assessment can contribute bounded meaning without making security/data/public-contract work automatically formal; typed `formalizationNeed` replaces prose-only escalation. Issue intake now accepts only validated planner/semantic output and derives triage facts from explicit labels/path evidence rather than issue prose. | Focused semantic/issue/routing suites; full non-system Vitest — 133 files passed, 564 tests passed, 7 skipped; `npx tsc --noEmit`; `npm run build`. | Real profile/provider evaluation remains pending and must run only in isolated packed candidates. | FIXED_AND_RECERTIFIED (deterministic boundary; provider integration pending) |
| AEH-V2-0019 | P1 | Frozen execution-selection boundary | Operation supervision, escalation and recovery could re-derive execution policy from a topology object inside lifecycle helpers. | `operations/supervisor.ts`, `agents/escalation.ts` and `agents/recovery.ts` accepted topology where caller-frozen selections or recovery policy were sufficient. | `src/operations/supervisor.ts`, `src/agents/escalation.ts`, `src/agents/recovery.ts`, `src/core/run.ts`, `src/operations/change.ts`, `src/audit/run.ts`, `src/agents/reviewLifecycle.ts`, `tests/supervisorBootstrapRegression.test.ts`, `tests/escalation.test.ts`, `tests/recovery.test.ts` | Supervisor, stage-selection and recovery tests pass with missing/invalid boundaries failing closed; lifecycle helpers no longer import `ResolvedAgentTopology`. | Core/change/audit resolve selections and recovery policy once; escalation consumes frozen stage/role/model selections; recovery consumes `RecoveryMap`; topology remains only at the outer resolution boundary. | Focused supervisor/escalation/recovery suite — 15 passed; `npx tsc --noEmit`. | Real supervisor/provider journey remains pending in isolated packed candidates. Reviewer discovery still uses topology at its outer caller boundary by design. | FIXED_AND_RECERTIFIED (deterministic boundary) |
| AEH-V2-0020 | P1 | Model-first issue and stack interpretation | Deterministic issue prose parsing and detector-first stack discovery could silently manufacture semantic scope, requirements or stack claims. | Issue normalization still had a deterministic plan fallback, while project discovery implicitly treated detector output as interpretation. | `src/issues/intake.ts`, `src/participants/stack.ts`, `src/core/errors.ts`, `tests/issueIntake.test.ts`, `tests/participantsStackDiscovery.test.ts` | Missing issue semantic planning blocks with a preserved raw snapshot; invalid issue assessment evidence is rejected; detector-disabled stack discovery returns `BLOCKED`; a digest-bound model assessment is accepted; explicit detector fast-path cache behavior remains covered. | Removed deterministic issue-contract fabrication and added typed normalization errors. Added objective stack evidence collection, validated `MODEL`/`HYBRID` stack assessments, evidence/assessment digests, unknown/blocked status and opt-in detector fast path. Inspection now exposes only frozen issue and explicit evidence; route selection waits for semantic normalization. | Focused issue/stack suites — 12 passed; full non-system Vitest — 133 files passed, 564 tests passed, 7 skipped; `npx tsc --noEmit`; `npm run build`. | Real semantic model execution and supported-provider stack journeys remain pending in isolated packed candidates. | FIXED_AND_RECERTIFIED (deterministic boundary; provider integration pending) |
| AEH-V2-0021 | P1 | Candidate impact / failure recovery semantics | Candidate impact and recovery classification used path/output heuristics as if they were semantic truth. | Candidate impact derived security/schema/API review dimensions from filenames; failure recovery classified provider/tool output with unbound regex interpretation. | `src/candidates/assembler.ts`, `src/agents/waveExecutor.ts`, `src/agents/recovery.ts`, `src/core/errors.ts`, `tests/candidateAssembler.test.ts`, `tests/recovery.test.ts` | Bound candidate impact claims are checked against observed ChangeSet paths; missing impact semantics require independent review; bound failure assessments are accepted; invalid evidence/digests are rejected; unknown output remains ambiguous. | Added evidence-bound `MODEL`/`HYBRID` CandidateImpact and FailureAssessment contracts. Existing planner output is reused for candidate impact without a duplicate classifier call; deterministic filename/provider markers remain conservative fallback only. | Focused candidate/recovery suites — 6 passed; full non-system Vitest — 133 files passed, 564 tests passed, 7 skipped; `npx tsc --noEmit`; `npm run build`. | Real model failure-assessment/provider journeys remain pending in isolated packed candidates. | FIXED_AND_RECERTIFIED (deterministic boundary; provider integration pending) |
| AEH-V2-0022 | P1 | KnowledgeGate/Librarian runtime integration | The canonical Librarian role was present, but the default topology advertised the Explorer output contract, and planner-wave compilation did not invoke KnowledgeGate for unknown competencies. | An unknown technology could reach participant compilation without a public-path knowledge resolution; the wrong output contract prevented a Librarian from producing a validated KnowledgePack. | `src/agents/outputContracts.ts`, `src/agents/waveExecutor.ts`, `src/architecture/participantPlan.ts`, `src/knowledge/index.ts`, `presets/agents/default.jsonc`, `tests/knowledgeGate.test.ts`, `tests/architectureWorkGraph.test.ts`, `tests/agentTopologyLayers.test.ts` | Planner competencies now pass through KnowledgeGate; unknown competencies use a bounded canonical Librarian lookup and operation-local pack digest; absent trusted lookup returns typed `KNOWLEDGE_GAP_BLOCKED`; forged pack/candidate provenance is rejected. | Added the dedicated `knowledge-pack` output contract, corrected the Librarian preset, integrated bounded lookup into planner-wave blueprint compilation, and projected validated pack references into ParticipantPlan. | Focused gate/compiler/topology/hygiene suites — 22 passed; full non-system Vitest — 133 files passed, 564 tests passed, 7 skipped; `npx tsc --noEmit`; `npm run build`. | Provider-backed Librarian/unknown-technology journey remains pending in an isolated packed candidate. | FIXED_AND_RECERTIFIED (deterministic boundary; provider integration pending) |
| AEH-V2-0023 | P1 | Frozen run execution boundary | `runTask` still carried a resolved topology through lifecycle execution and re-derived recovery/reroute choices during remediation, allowing mutable topology-shaped state to remain an implicit runtime authority. | Route, participant selections, execution catalog and recovery policy were not returned as one frozen boundary; remediation could consult topology-derived policy after execution began. | `src/core/run.ts`, `src/architecture/executionCatalog.ts`, `tests/executionCatalog.test.ts`, `tests/operationLifecycleRegression.test.ts`, `tests/recovery.test.ts` | Typecheck, build, focused lifecycle/recovery tests and the full non-system suite pass after the boundary migration; packaged user-turn smoke also passes. | `runTask` now resolves topology once at the outer boundary and consumes a `FrozenExecutionBoundaryV1` containing route, selections, compiled `ExecutionCatalog`, `RecoveryMap` and reroute bindings. Lifecycle stages no longer receive or re-derive topology. | `npm run typecheck`; `npm run build`; non-system Vitest — 133 files passed, 564 tests passed, 7 skipped; packed human black-box user-turn smoke — 1 passed. | Real supervisor/provider journey remains pending in isolated packed candidates. | FIXED_AND_RECERTIFIED (deterministic boundary; provider integration pending) |
| AEH-V2-0024 | P1 | Unknown-stack semantic grounding | Objective evidence collection preserved an unseen repository, but the semantic stack profile could not represent an interpreted language outside the built-in fast-path vocabulary; supported semantic claims could also omit grounding refs. | `ProjectLanguageV1` was a closed detector vocabulary and the generic semantic payload permitted ungrounded supported claims. | `src/participants/stackTypes.ts`, `src/participants/stack.ts`, `src/semantic/assessment.ts`, `tests/participantsStackDiscovery.test.ts`, `tests/semanticAssessment.test.ts` | An unseen Cargo/Rust-like repository remains `BLOCKED` without interpretation, accepts a digest/evidence-bound `rust` semantic profile without adding a detector, and rejects supported claims without evidence. | Semantic stack profiles now preserve arbitrary normalized language labels while built-in detectors remain optional fast paths; semantic payload validation requires evidence for supported/conflicting claims and recommendations. | `npm run typecheck`; focused stack/semantic suites — 12 passed. | Real unknown-technology model/Librarian journey remains pending in an isolated packed candidate. | FIXED_AND_RECERTIFIED (deterministic boundary; provider integration pending) |
| AEH-V2-0025 | P1 | KnowledgeGate pack-to-skill continuity | A Librarian could return a valid sufficient KnowledgePack without the optional explicit skill candidate, leaving the planner gate marked sufficient while participant compilation still lacked an operation-local competency skill. | Pack-only lookup results were not normalized through the same high-confidence candidate derivation used by cache hits and direct pack lookups. | `src/knowledge/index.ts`, `tests/knowledgeGate.test.ts`, `tests/architectureWorkGraph.test.ts` | A pack-only Librarian result derives a trusted candidate bound to the validated pack; invalid explicit candidates remain rejected. | KnowledgeGate now normalizes supplied-or-derived candidates through one validation path before returning `SUFFICIENT`. | `npm run typecheck`; focused KnowledgeGate/compiler suites — 14 passed. | Provider-backed Librarian journey remains pending in an isolated packed candidate. | FIXED_AND_RECERTIFIED (deterministic boundary; provider integration pending) |
| AEH-V2-0026 | P1 | Knowledge provenance and competency coverage | A cached or looked-up KnowledgePack could be accepted based on cache-key shape while its digest or high-confidence claims did not cover the requested competency. | Cache writes did not validate pack integrity, and resolution did not revalidate cached packs or require one trusted claim per missing competency before deriving a skill candidate. | `src/knowledge/index.ts`, `tests/knowledgeGate.test.ts` | Poisoned cached and looked-up packs are rejected; pack-only results still derive a candidate only after the same validation path. | KnowledgeGate validates pack digests on cache insertion and resolution, requires high-confidence coverage for every requested gap, and validates explicit/derived skill candidates against the pack. | `npm run typecheck`; full non-system Vitest — 133 files passed, 568 tests passed, 7 skipped; packed scenario/adversarial/concurrency/human slices — 64 passed. | Provider-backed Librarian and real model evidence remain pending in isolated packed candidates. | FIXED_AND_RECERTIFIED (deterministic boundary; provider integration pending) |
| AEH-V2-0027 | P1 | Local project and UI isolation | Symlinked repository evidence could read outside the project, a symlinked Control Center asset could escape the UI root, a symlinked registered project could appear available, and a persisted non-loopback health URL could trigger an arbitrary outbound probe. | Local-boundary checks used lexical paths or unrestricted file/fetch behavior without enforcing realpath identity and loopback-only health targets. | `src/participants/stack.ts`, `src/projects/registry.ts`, `src/control-center/server.ts`, `tests/participantsStackDiscovery.test.ts`, `tests/projectRegistry.test.ts`, `tests/controlCenter.test.ts` | Outside-target symlinks are not read/served, redirected project roots are marked moved-or-missing, malformed frontend paths fail closed, and non-loopback health records are reported unreachable without probing. | Evidence collection skips symlink content/traversal; registry availability compares realpath identity; static frontend serving rejects symlinks and outside-root targets; health registration/probing is constrained to loopback addresses. | `npm run typecheck`; focused stack/registry/Control Center suites — 26 passed; full non-system Vitest — 133 files passed, 569 tests passed, 7 skipped; `npm run build`. | Packed loopback Control Center smoke passed; external service/network lanes remain unclaimed. | FIXED_AND_RECERTIFIED (deterministic + loopback) |
| AEH-V2-0028 | P1 | Validation need versus validation execution | Planner output represented validation as free-form gate strings, so semantic “what must be demonstrated” could not be carried into the frozen execution blueprint or deterministically resolved to an approved action. | `validationGates` was advisory metadata; no typed requirement schema or resolver connected project scripts, configured validators/providers and policy to the execution boundary. | `src/architecture/validationRequirements.ts`, `src/architecture/participantPlan.ts`, `src/agents/outputContracts.ts`, `src/agents/waveExecutor.ts`, `src/core/errors.ts`, `tests/architectureWorkGraph.test.ts`, `tests/scale-governance.test.ts` | Typed validation requirements resolve to an approved project script and block when no approved action exists; planner schemas and blueprint contracts remain aligned. | Added `ValidationRequirementV1` and `ValidationResolutionV1`; the resolver chooses only project-authoritative scripts, configured commands/validators or approved providers, with optional ToolRegistry availability checks. Unresolved requirements fail closed with `VALIDATION_REQUIREMENT_BLOCKED`; model output cannot provide commands, tools, providers or credentials. | `npm run typecheck`; focused architecture/schema suite — 26 passed; full non-system Vitest — 133 files passed, 569 tests passed, 7 skipped; `npm run build`. | Real project-specific provider resolution remains pending in isolated packed candidates. | FIXED_AND_RECERTIFIED (deterministic boundary; provider integration pending) |
| AEH-V2-0029 | P0 | Parallel wave candidate lineage | Sibling workers in one wave all observed the same base candidate, but sequential integration relabelled later ChangeSets with the revision advanced by earlier siblings, and wave worktrees were materialized from HEAD + prior patches rather than from the frozen candidate. | Wave assembly used the moving `currentCandidate` for each sibling and had no explicit wave base, no derivation record and no reconciliation outcome. | `src/candidates/wave.ts`, `src/candidates/assembler.ts`, `src/candidates/direct.ts`, `src/agents/waveExecutor.ts`, `src/distributed/worker.ts`, `tests/candidateWaveLineage.test.ts`, `tests/candidateAssembler.test.ts` | Independent siblings, clean same-file rebase, unresolvable conflict, stale base, digest mismatch, superseded wave base, deterministic ordering, retry against the advanced candidate, resource-claim conflict and post-apply assembly failure are all covered. | Added frozen `WaveBaseV1`; ChangeSets now carry `baseCandidateDigest` and are never rewritten; later siblings produce explicit `WAVE_REBASE` derived ChangeSets; unresolved siblings return typed `WAVE_RECONCILIATION_REQUIRED`; wave worktrees materialize the exact candidate state and verify its digest; failed assembly rolls the workspace back to the bound candidate. Distributed jobs carry and verify the frozen base digest. | `npm run typecheck`; `npx vitest run tests/candidateWaveLineage.test.ts` — 10 passed; full non-system Vitest — 140 files passed, 1 skipped; 675 tests passed, 8 skipped. | Real multi-participant wave journey remains pending in isolated packed candidates. | FIXED_AND_RECERTIFIED (deterministic boundary; provider integration pending) |
| AEH-V2-0030 | P1 | WorkUnit logical resource claims | Path independence was treated as execution independence, so two units could mutate the same logical resource (schema, migration sequence, lockfile, environment) concurrently. | No WorkUnit-level resource model existed; the scheduler only reasoned about scope overlap and graph proximity. | `src/architecture/workGraph.ts`, `src/agents/parallelism.ts`, `src/agents/outputContracts.ts`, `src/agents/waveExecutor.ts`, `tests/architectureWorkGraph.test.ts` | Claim validation, conflict semantics, duplicate-order detection, exclusive-write serialization, shared-read co-scheduling and ordered-sequence wave ordering are covered. | Added `SHARED_READ` / `EXCLUSIVE_WRITE` / `ORDERED_SEQUENCE` claims with deterministic conflict and ordering helpers; the scheduler serializes conflicting units and enforces ordered sequences across waves; the wave integrator re-checks claims as defense in depth. | `npm run typecheck`; `npx vitest run tests/architectureWorkGraph.test.ts` — 14 passed; full non-system Vitest — 140 files passed, 1 skipped; 675 tests passed, 8 skipped. | No distributed lock system was introduced by design; cross-host claims are not claimed. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0031 | P0 | Controller fencing | A controller process whose wall-clock lease had not expired could keep mutating an operation after another controller had taken over; controller identity was self-declared. | There was no durable monotonic controller identity, no secret controller proof, and candidate/terminal/tool-action mutations did not verify controller ownership. | `src/operations/state.ts`, `src/operations/controller.ts`, `src/security/executionLease.ts`, `src/security/toolActionGate.ts`, `src/architecture/participantPlan.ts`, `src/agents/waveExecutor.ts`, `tests/security/controllerFencing.test.ts` | Zombie candidate binds, terminal transitions, execution authority, blueprints, tool actions and receipt recording are rejected; a forged token with the right epoch is rejected; candidate revision jumps are rejected. | Added durable `controller.epoch` + a secret controller token whose digest is the only durable copy; every takeover increments the epoch and mints a new token; all controlled mutations and controller-authority tool actions verify epoch and token; execution authority, blueprints and action intents carry the epoch. | `npm run typecheck`; `npx vitest run tests/security/controllerFencing.test.ts` — 8 passed; full non-system Vitest — 140 files passed, 1 skipped; 675 tests passed, 8 skipped. | Same-user filesystem forgery of operation state remains outside the threat model; a hostile same-user process can still re-claim ownership. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0032 | P0 | ToolActionGate public enforcement and reconciliation | The deterministic tool action gate existed but did not cover all public delivery mutations, and a crash between side effect and persistence could cause an unsafe retry. | The public issue auto-handoff path ran before the controller AcceptanceOracle; workspace creation was bundled into public issue/branch setup; uncertain effects lacked a complete common intent/reconciliation path. The standalone `aeh sdd handoff` CLI also advertised public delivery but invoked a managed-operation-only handoff without managed acceptance context. | src/security/toolActionGate.ts, src/security/actionPolicy.ts, src/security/gatedAction.ts, src/security/actionReconciliation.ts, src/delivery/finalize.ts, src/delivery/handoff.ts, src/issues/workflow.ts, src/operations/controller.ts, src/core/run.ts, tests/deliveryFinalize.test.ts, tests/security/actionPolicy.test.ts, tests/security/gatedAction.test.ts, tests/security/toolActionGate.test.ts, tests/security/actionReconciliation.test.ts, src/cli.ts, tests/cliS8Handoff.test.ts | Every configured requested public effect waits for the current AcceptanceOracle and policy-required S7 gate; each sensitive action persists intent, passes ToolActionGate, and persists an ActionReceipt or safe reconciled equivalent. Controller-owned Paseo execution workspaces are local resources, not delivery effects, while preserving S3 fencing and conservative HUMAN_REQUIRED reconciliation. | S8 preserves the strict acceptance boundary: default issue intake starts a managed controller run without public handoff; controller-owned disposable Paseo workspaces are LOCAL_RESOURCE_CREATION, absent from configured external effects, but remain authority-fenced and intent/receipt/reconciliation tracked. After current acceptance and policy-required S7 verification, handoff/finalization gates public GitHub issue/branch/push/PR effects and persists receipts or safe reconciled receipts. Executor uncertainty is inspected before retry; unresolved or unknown Paseo identity remains HUMAN_REQUIRED. The normal standalone `sdd handoff` command now reports `SDD_HANDOFF_UNSUPPORTED` before config loading and cannot enter direct delivery; managed accepted finalization is unchanged. | Final focused S8 action/delivery/issue/CLI command — 14 files / 198 tests; `npx tsc --noEmit` exit 0; `npm run build` exit 0 (`release-1790322187860-3232428-0507f39b`); full non-system Vitest — 162 files passed, 1 skipped; 1,014 passed, 7 skipped; scenario/concurrency system — 2 files / 17 passed; `git diff --check` clean; normative target equals HEAD. The default issue-flow fixture stubbed GitHub and pushed only to a local bare repository. CLI regression passed 2 tests; the separate Flash baseline failed 2/2 against the stale surface and passed 2/2 against the remediation. | No real GitHub/Paseo provider certification. Paseo reconciliation remains HUMAN_REQUIRED when provider identity cannot be safely confirmed. S8 was independently accepted on its deterministic gate by the Campaign Director on 2026-09-25; no S9 work had started at acceptance. | FIXED_AND_DETERMINISTICALLY_VERIFIED (S8 lead gate; external/provider lanes pending)  |
| AEH-V2-0033 | P1 | Paseo session binding identity | Paseo session identity was inferred from provider ids without a durable binding to AEH operation/participant/blueprint identity, so a reused session could be attributed to stale semantic state. | The binding was partial and had no production runtime consumer. | `src/paseo/sessionBinding.ts`, `src/workers/agentPrompt.ts`, `tests/paseo/sessionBinding.test.ts`, `tests/agentPromptStructuredResultProvenance.test.ts` | Exact-field schema, digest/path corruption, idempotent bind, conflict/stale drift, rotation, active status, participant isolation, and concurrent rotation pass; production prompt preparation binds or validates the actual provider session before dispatch. | `PaseoSessionBindingV1` now binds full canonical operation/revision, participant/generation, candidate, blueprint, policy, context/prompt, controller epoch, and actual Paseo agent identity. A different session rotates; an identity mismatch on the same live session rejects reuse rather than relabeling it. | `npx vitest run tests/paseo/sessionBinding.test.ts` — 17 passed; full non-system Vitest — 320 files, 830 passed, 7 pending; `npx tsc -p tsconfig.json --noEmit` and `npm run build` passed. | No REAL_PROVIDER session journey or certification is claimed. | FIXED_AND_RECERTIFIED (S5 deterministic binding/runtime); provider behavior pending |
| AEH-V2-0034 | P2 | Candidate self-dependency | The development checkout declared a released copy of itself as a devDependency, so an old AEH release could be resolved while developing the candidate. | `package.json` and `package-lock.json` pinned `agentic-engineering-harness@^0.6.29`. | `package.json`, `package-lock.json`, `tests/architectureHygiene.test.ts` | A hygiene test asserts no dependency/lock entry for the package's own name. | Removed the self-dependency and regenerated the lockfile; the repo-local bin shim is provided by `scripts/link-self-bin.mjs`. | `npm install --package-lock-only --offline`; `npx vitest run tests/architectureHygiene.test.ts`. | None. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0035 | P1 | Candidate impact mechanism labelling and receipt integrity | Planner-derived impact claims were labelled `HYBRID` with no unknowns, an orphaned receipt could make the gate re-execute a non-idempotent action, and a candidate revision jump could be bound without lineage. | Impact assessment hardcoded `mechanism: "HYBRID"`; `authorizeToolAction` did not inspect an intent-less receipt; `bindOperationCandidate` allowed arbitrary revision jumps. | `src/agents/waveExecutor.ts`, `src/security/toolActionGate.ts`, `src/operations/state.ts`, `tests/security/toolActionGate.test.ts`, `tests/security/controllerFencing.test.ts` | Planner impact is now `MODEL` with explicit unknowns; an intent-less receipt yields `TOOL_ACTION_RECEIPT_ORPHANED`; non-placeholder binds must advance exactly one revision. | Labeled planner claims honestly, added orphan-receipt fail-closed behavior and operation-context enforcement for receipts, and enforced revision+1 lineage. | `npx vitest run tests/security/toolActionGate.test.ts tests/security/controllerFencing.test.ts`. | `requiresIndependentReview` is still not consumed by reviewer compilation. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0036 | P2 | Provenance scenario fixture | The packed system scenario matrix expected a hand-written provenance manifest to verify while omitting the now-required BuildIdentity, so the P10 truth-property assertion failed. | The fixture predated the BuildIdentity hardening of `verifyProvenanceManifest`. | `tests/system/aehScenarioMatrix.test.ts` | The scenario matrix now supplies a valid BuildIdentity and still proves that source tampering breaks provenance. | `npx vitest run tests/system/aehScenarioMatrix.test.ts` — 8 passed; `npm run test:system-all` rerun. | None. | FIXED_AND_RECERTIFIED (deterministic) |
| AEH-V2-0037 | P0 | Frozen execution and skill identity | Participant launches and structured results lacked one complete immutable v2 identity across policy, role ceiling, blueprint, runtime binding, actual prompt/context digests, and accepted ephemeral procedure content; individual procedure steps were not required to link pack evidence. Durable generic mutation paths also allowed stale-digest in-place mutation of frozen policy and participant binding bodies through `updateOperationMetadata` callbacks. The Director also reproduced generic patching of a future executionSemanticsDigest followed by a same-digest owner no-op, which left policy/bindings stale; a separate durable-record reproduction showed a missing operationExecutionRevision could be generically upgraded to 1. | Identity was spread across optional provenance fields, and the SkillCandidate trust boundary accepted aggregate pack/competency coverage without grounded evidence references for every procedure step. The generic participant updater could add a bound participant, while its transition check reconstructed binding identity from mutable post-callback participant maps; deletion/re-keying could alias both sides. Separately, policy and binding identity needed a pre-callback snapshot to prevent same-digest body changes from crossing persistence. The transition guard omitted executionSemanticsDigest, checked revision changes only when the mutable post-callback record already had a revision, and did not snapshot scalar identity presence/value before callbacks. | `src/architecture/executionIdentity.ts`, `src/architecture/participantPlan.ts`, `src/operations/state.ts`, `src/agents/outputContracts.ts`, `src/knowledge/index.ts`, `src/workers/agentPrompt.ts`, `src/workers/runtimeSessions.ts`, `src/workers/resultGateway.ts`, `src/distributed/worker.ts`, `src/paseo/launchSpec.ts`, `src/paseo/runtimeCore.ts`, `src/paseo/sdk.ts`, `tests/operations.test.ts`; S1 identity/provenance tests. | Full policy/role/blueprint/binding and grounded SkillManifest digests are compiled, frozen, and checked at launch/result boundaries. The expanded compiler blueprint is deeply immutable; direct OpenCode/Codex, Podman/OpenCode, Paseo, and distributed launch paths bind an actual pre-dispatch runtime session and check the returned identity. Operation state captures participant IDs and each complete canonical binding body before callbacks, then compares every ID in the pre-callback/post-callback union; generic add/replace/mutate/clear attempts fail closed. Only the validated `operation.participant.execution-bound` event may create/replace one binding, while candidate assembly, takeover, and changed execution-semantics events may only clear. The complete canonical ResolvedOperationPolicy snapshot and its explicit bind/invalidation lifecycle remain enforced. Procedure-text, trust-decision-digest, participant-binding, same-digest policy/body, stale-digest in-place `runtime.sessionId`, participant delete/re-key, and durable-file-byte-preservation regressions are asserted. The guard now snapshots semantics digest and execution-revision presence/value before callbacks; only the semantics owner can change the digest, first/identical binds preserve execution revision, and replacing bound semantics advances once while clearing frozen policy and every participant execution binding. Generic events cannot synthesize a missing revision, and old records remain unsupported at execution boundaries. | Candidate/fencing prerequisite command `npx vitest run tests/candidateIdentity.test.ts tests/security/controllerFencing.test.ts` — 2 files / 12 tests passed. Before the revision/digest fix, the Flash worker ran the operation suite against the unremediated state: 1 file failed, 4 failed / 29 passed (33), with failures in the four intended bypass groups. Post-fix `npx vitest run tests/operations.test.ts` — 1 file / 33 tests passed; focused S1 — 15 files / 165 tests passed; `npx tsc --noEmit` — exit 0; `npm run build` — exit 0; `npx vitest run --exclude 'tests/system/**'` — 149 files passed, 1 skipped; 773 tests passed, 7 skipped; `git diff --check` clean. Full-suite build-hygiene tests passed with missing-source-map notices. | No REAL_PROVIDER certification. Adapter and session behavior was captured at deterministic boundaries; provider execution remains pending. | FIXED_AND_RECERTIFIED (S1 deterministic adapter boundary; provider execution pending) |
| AEH-V2-0038 | P2 | CandidateImpact provenance boundary | Candidate assembly and wave inputs still accepted a precomputed CandidateImpact object that could bypass canonical SemanticAssessmentService validation of agent/profile, Paseo session, read receipts, policy, and evidence provenance. | A legacy optional `impactAssessment` field on internal assembly/submission APIs projected caller-supplied claims into production impact output after only structural and digest checks. | `src/candidates/assembler.ts`, `src/candidates/wave.ts`, `src/agents/waveExecutor.ts`, `tests/candidateAssembler.test.ts`, `tests/candidateWaveLineage.test.ts` | Tests verify canonical service assessment against the assembled candidate and exact file receipts, rollback on invalid changed-path judgment, and a BLOCKED independent-review minimum when no runtime is supplied. | Removed the precomputed impact input from assembly and wave APIs. Only the canonical service can produce MODEL impact; absent runtime remains explicitly BLOCKED and keeps independent review required. | S2 focused suites — 14 files / 108 tests passed; `npx tsc --noEmit` — exit 0; `npm run build` — exit 0; full non-system Vitest — 151 files passed, 1 skipped; 788 tests passed, 7 skipped. | No REAL_PROVIDER execution or certification; fakes verify deterministic contracts only. | FIXED_AND_DETERMINISTICALLY_VERIFIED (S2; S4 downstream assurance consumption remains future scope) |
| AEH-V2-0039 | P1 | STACK repository evidence and proposed path boundary | STACK receipts could be stamped with another root's binding, stale content could be scanned during drift, ignored secret/runtime files could enter evidence, and model-proposed `projectSkillRoots` could contain absolute, traversing, malformed, missing, or escaping paths. | The scanner inventoried its own filesystem root without comparing canonical root/content digests and did not use Git's tracked/non-ignored file set; projected skill-root strings were copied without generic path/existence/containment validation. | `src/core/git.ts`, `src/semantic/runtime.ts`, `src/participants/stack.ts`, `src/semantic/assessment.ts`, `tests/participantsStackDiscovery.test.ts`, `tests/semanticAssessmentRuntime.test.ts` | Regressions reject cross-root substitution, pre-capture and in-assessor drift, missing STACK root digest, ignored `.env`/`.harness/cache` references, and invalid project skill-root paths; valid in-root roots remain descriptive. | Binding creation hashes canonical realpath. STACK requires and compares that root digest plus the worktree digest before scan, after evidence capture, after assessment and before return; evidence inventory uses the worktree digest's Git file set or stricter generic exclusions. Project skill roots require normalized relative syntax, existence and realpath containment and cannot grant skills/capabilities. | Focused S2 suites — 14 files / 123 tests passed; stack/semantic runtime set — 5 files / 75 tests passed; `npx tsc --noEmit` — exit 0; `npm run build` — exit 0; full non-system Vitest — 151 files passed, 1 skipped; 803 tests passed, 7 skipped. | No REAL_PROVIDER, BROWSER, or ADVERSARIAL evidence; all checks use deterministic fixtures. | FIXED_AND_DETERMINISTICALLY_VERIFIED (S2; provider evidence pending) |
| AEH-V2-0040 | P1 | High-risk semantic INTENT capability ceiling | Every semantic INTENT request with `risk: "high"` was rejected during policy validation even though the caller explicitly supports HIGH and downstream route/assurance logic supplies conservative floors. | `semanticCapabilityPolicyV1.INTENT.maxRiskClass` was STANDARD while the public caller mapped high-risk input to HIGH. | `src/semantic/assessment.ts`, `src/audit/intent.ts`, `tests/auditIntent.test.ts` | Focused regressions assert HIGH is admitted within existing request budgets and keeps deterministic CRITICAL assurance and delegation floors. | Raised only the INTENT maximum risk ceiling to HIGH. Reasoning/context/token/time bounds and deterministic routing/assurance floors are unchanged. | Focused S2 suites — 14 files / 123 tests passed; `npx tsc --noEmit` — exit 0; `npm run build` — exit 0; full non-system Vitest — 151 files passed, 1 skipped; 803 tests passed, 7 skipped. | No REAL_PROVIDER or certification claim. | FIXED_AND_DETERMINISTICALLY_VERIFIED (S2; provider evidence pending) |
| AEH-V2-0041 | P1 | CandidateImpact assurance recompilation | Actual post-assembly impact was not consumed to compile policy floors, validation requirements, actual independent Reviewer assignments, candidate-bound acceptance assertions, or evidence strength. Impact flags alone did not create independent review evidence. | CandidateImpact was provenance-bound by S2, but downstream run, wave, repair, validation, and review lifecycles did not deterministically recompile requirements against the current CandidateRevision and frozen ResolvedOperationPolicy. | `src/architecture/candidateAssurance.ts`, `src/candidates/assembler.ts`, `src/candidates/repair.ts`, `src/core/run.ts`, `src/agents/reviewLifecycle.ts`, `tests/candidateAssurance.test.ts`, `tests/candidateAssembler.test.ts`, `tests/runRepairCandidateLifecycle.test.ts`, `docs/CORE_ARCHITECTURE_V2_S4_WORKGRAPH.md` | Regressions cover policy floors, stale/tampered impact and resolver binding, absent/disallowed required validations, high-risk unknowns, reviewer eligibility/diversity, actual Reviewer session identity, candidate assertion/evidence-strength binding, and repair lifecycle recompile. | CandidateImpact now binds candidate/base/patch facts; deterministic compiler and lifecycle consume exact current impact/policy, execute required validation, assign actual independent read-only Reviewers, persist candidate-bound assertions/evidence strength, and fail closed. No S6 AcceptanceOracle or normative target change. Delegation assessment and exact profile/worktree evidence are recorded in `docs/CORE_ARCHITECTURE_V2_S4_WORKGRAPH.md`. | Focused `npx vitest run tests/candidateAssurance.test.ts tests/candidateAssembler.test.ts tests/reviewLifecycle.test.ts tests/reviewRepairCandidateLifecycle.test.ts tests/runRepairCandidateLifecycle.test.ts tests/candidateRepairProtection.test.ts tests/candidateWaveLineage.test.ts` — exit 0, 7 files / 41 tests; `npm run typecheck` — exit 0; `npm run build` — exit 0 (`release-1790299120251-2597132-058f35f2`); `npx vitest run --exclude 'tests/system/**'` — exit 0, 153 files passed, 1 skipped; 848 tests passed, 7 skipped; `git diff --check` clean. No matching S4 system suite was found; the deterministic public `runTask` integration test exercises the slice. | No REAL_PROVIDER, BROWSER, OPA_PROVIDER, ADVERSARIAL, or PACKED_E2E claim. S6 acceptance oracle and live semantic/provider behavior remain pending. Flash worker `b6f54a20-9bd9-45bd-ae6b-fea824586940` used DeepSeek V4.1 Flash Implementer in the separate `s4-candidate-assurance-flash` Paseo worktree; no child agents, commit, or push. | FIXED_AND_DETERMINISTICALLY_VERIFIED (S4 gate; S6 oracle and provider lanes pending) |
| AEH-V2-0042 | P1 | AcceptanceOracle and objective completion | Managed run/change success could proceed from validation/review success without a persisted current AcceptanceOracle disposition, managed Lead evidence ordering, or deterministic objective Definition of Done; delivery could start before managed acceptance. | S4 produced bound assertions and assurance strength, but the production path did not resolve each assertion to sufficient provenance-bound evidence, persist a controller-owned disposition, enforce the disposition before delivery, or re-evaluate the complete objective evidence set at terminalization. | `src/architecture/acceptanceOracle.ts`, `src/architecture/objectiveCompletion.ts`, `src/agents/managedLeadAcceptance.ts`, `src/core/run.ts`, `src/delivery/finalize.ts`, `src/operations/controller.ts`, `src/operations/state.ts`, `tests/acceptanceOracle.test.ts`, `tests/managedLeadAcceptance.test.ts`, `tests/objectiveCompletion.test.ts`, `tests/objectiveCompletionLifecycle.test.ts`, `tests/deliveryFinalize.test.ts` | Regressions cover validated current policy/epoch identity, takeover digest rotation and old-evidence rejection, required assertion evidence strength, actual Reviewer execution/session provenance, bound managed Lead evidence, epoch-zero DoD, invalid/stale evidence, certification omission when not required, exact persisted-oracle/objective coverage matching, terminalization, and delivery blocking without accepted current disposition. | S6 resolves VerificationRequirements from unchanged S4 assertions, builds and persists current EvidenceBundles and deterministic ACCEPTED/REJECTED controller dispositions, requests bound Lead semantic evidence where frozen policy requires it, gates delivery effects, and recomputes complete objective DoD including WorkGraph/participant accounting at terminalization. The normative target, CertificationCore responsibility, S3 authority semantics, and S4 assertion/evidence-strength contracts were not changed. | Focused S6 `npx vitest run tests/objectiveCompletion.test.ts tests/objectiveCompletionLifecycle.test.ts tests/acceptanceOracle.test.ts tests/managedLeadAcceptance.test.ts tests/candidateAssurance.test.ts tests/runRepairCandidateLifecycle.test.ts tests/reviewLifecycle.test.ts tests/deliveryFinalize.test.ts tests/operationV2Identity.test.ts tests/operationLifecycleRegression.test.ts tests/operationSupervisionArchitecture.test.ts` — 11 files / 124 tests passed; `npx tsc --noEmit` — exit 0; `npm run build` — exit 0 (`release-1790303504564-2721309-64c0f9cc`); full non-system Vitest — 157 files passed, 1 skipped; 931 passed, 7 skipped; relevant system scenario/concurrency tests — 2 files / 17 passed; `git diff --check` — clean. | No REAL_PROVIDER, BROWSER, OPA_PROVIDER, ADVERSARIAL, or complete PACKED_E2E acceptance/delivery certification. The Campaign Director independently accepted the deterministic S6 gate on 2026-09-24 after reviewing the final diff and rerunning the focused, typecheck, build, full non-system, and relevant system gates. No external certification is claimed. | FIXED_AND_DETERMINISTICALLY_VERIFIED (S6 deterministic gate; external evidence lanes pending) |

| AEH-V2-0043 | P1 | Supply-chain provenance gate | Required provenance verification did not receive the current CandidateRevision or configured packed-artifact reference, and generated evidence did not form one deterministic candidate/build/artifact/policy/builder/SBOM/signature chain before delivery. | Version-1 provenance carried file digests and optional attestations but had no mandatory exact S7 bindings; strict verification selected evidence by filename rather than the policy-selected artifact, the default SBOM scan targeted the repository root, and delivery called the gate without current candidate/artifact context. | `src/provenance/generate.ts`, `src/build/identity.ts`, `src/core/config.ts`, `src/core/types.ts`, `schemas/project.schema.json`, `src/delivery/finalize.ts`, `src/cli.ts`, `src/core/doctor.ts`, `tests/provenance.test.ts`, `tests/provenanceS7.test.ts`, `tests/deliveryFinalize.test.ts`, `tests/system/aehAdversarialE2E.test.ts` | Deterministic regressions cover missing chain elements, malformed/tampered artifacts and SBOM, wrong/stale candidate/build/policy/builder bindings, statement/predicate/subject mismatch, configured-key signing boundary, explicit retired-alias rejection, non-strict behavior, and no delivery request or commit when required evidence is missing. | Version-2 evidence binds exact current candidate, policy-selected packed artifact and digest, running BuildIdentity, supply-chain policy digest, expected builder, SBOM and in-toto/SLSA evidence, and policy-required Cosign key verification. Strict delivery checks after the persisted S6 AcceptanceOracle and before effects. Old manifest versions and `cosignKey` alias have no compatibility path. No target wording, acceptance, authority, or S8 subsystem changed. | Focused `npx vitest run tests/provenance.test.ts tests/provenanceS7.test.ts tests/architectureIntegrationContracts.test.ts tests/deliveryFinalize.test.ts --reporter=dot` — 4 files / 49 passed; typecheck exit 0; build exit 0 (`release-1790308464438-2905085-da2062aa`); full non-system — 158 files passed, 1 skipped; 965 passed, 7 skipped; S7-specific adversarial missing-manifest system case — 1 passed / 27 skipped; schema JSON and `git diff --check` clean. Three-file broader system run had 43 passed / 2 failed in unrelated terminal-operation fixtures missing required S6 objective/AcceptanceOracle evidence. | No REAL_PROVIDER signing was run because the `cosign` CLI is unavailable in this environment. The S7 fake Cosign/PATH-shim bundle is explicitly marked deterministic fixture data and cannot establish provider certification. | FIXED_AND_DETERMINISTICALLY_VERIFIED (S7 deterministic gate accepted by Campaign Director 2026-09-24; provider certification pending) |
| AEH-V2-0044 | P2 | S7 provenance schema conditions | Optional/non-strict provenance configuration could be rejected because the schema conditions applied required constraints when their relevant flags were absent. | JSON Schema properties checks do not fail when an instance property is absent, and the allOf if branches lacked required guards at the provenance object and nested policy object. | schemas/project.schema.json, src/core/config.ts, templates/project.yaml, tests/projectSchemaS7.test.ts | Structural regressions require parent and nested required guards for every policy flag; existing config-loader cases accept the template/non-strict policies and valid strict policy, and reject missing artifact or required key material. | Added parent and nested required guards to both provenance condition groups. Runtime Zod policy and template semantics are unchanged; no JSON Schema validator dependency was added. | Pre-fix Flash worktree run: 1 file failed, 6 structural guard tests failed, 15 passed. Post-fix focused S7 set: 5 files / 70 tests passed; schema-specific suite 21 passed; Campaign Director independently reran TypeScript noEmit (exit 0), build (exit 0; release-1790310426337-17-0e3b4825), full non-system suite (159 files passed, 1 skipped; 986 passed, 7 skipped), scenario matrix (8 passed), isolated adversarial missing-manifest case (1 passed / 27 skipped), schema JSON parse, and diff checks. | No JSON Schema validator is installed/resolvable. No REAL_PROVIDER signing claim. The previously recorded two broader adversarial terminal-fixture failures remain the known S6 objective-completion/AcceptanceOracle setup issue; this remediation did not change them. | FIXED_AND_DETERMINISTICALLY_VERIFIED (S7 deterministic gate accepted by Campaign Director 2026-09-24) |
| AEH-V2-0045 | P1 | Runtime supervision and Control Center projection | The initial S9 review found that managed provider leases were not consumed by a production provider/session adapter, and the Control Center projection omitted typed stage/blocker/participant/runtime detail and a cancellation control. | Generic managed lease operations and fixture UI callbacks did not establish production Paseo lifecycle ownership or durable operation decision/resume behavior. | `src/runtime/supervisorV2.ts`, `src/runtime/managed.ts`, `src/runtime/providerLifecycle.ts`, `src/paseo/runtimeCore.ts`, `src/paseo/runtimeInitialTurn.ts`, `src/operations/controller.ts`, `src/control-center/operationProjection.ts`, `src/control-center/contracts.ts`, `src/entry.ts`, `ui/control-center/src/api.ts`, `ui/control-center/src/App.tsx`, `tests/paseoProviderLeaseLifecycle.test.ts`, `tests/paseoInitialTurnBarrier.test.ts`, `tests/packed/s9ProviderLifecycleCampaign.mjs`, Control Center suites | Deterministic tests cover concurrent conflicting lease owners, durable acquire/renew/release, expiry fencing, stale controller/identity rejection, uncertain provider retention, exact-session takeover, bound Lead-generation checks, unsupported unleased fallback rejection, stale-controller stop fencing, same-controller overlapping-write exclusion, cancellation cleanup, durable semantic projection, and paired scoped decision/replay boundaries. The packed campaign kills a child owner, verifies newer-epoch takeover after exact-session idle observation, injects a provider stop failure and verifies the operation stays RUNNING with its lease retained, then completes scoped restart cancellation only after stop/re-observation and lease release. | Production operation-owned Paseo SDK materialization and participant/bound-Lead turns now use the managed provider lease API; each lifecycle action has a unique owner, while current operation/candidate/execution revision/policy/token/epoch and S1/S5 session identity remain authoritative. Operation-owned materialization fails closed if only the unleased CLI lifecycle is available. Control Center projections and paired controls remain read-only/server-routed. No normative architecture, S3 authority, S5 session/context, or S8 public-effect acceptance boundary changed. | Final remediation focused command — 13 files / 137 tests passed; `npm run typecheck`, `npm run ui:build`, and `npm run build` exited 0 (direct build `release-1790334556192-3490030-97632a32`); `tests/system/aehConcurrencyCampaign.test.ts` — 1 file / 9 tests passed (source-importing system lane); `npm pack` prepare and the final R5 packed campaign passed against `/tmp/s9-packed-final4/agentic-engineering-harness-0.8.4.tgz`, extracted to `/tmp/s9-packed-fixture-final4/package`. The final tarball digest is included in the S9 handoff report. Prior fixture browser choice/cancel results remain BROWSER_FIXTURE only. | The packed/provider calls used deterministic inspect/stop stubs, so REAL_PROVIDER lifecycle certification is unclaimed. The direct Paseo browser bridge returned `No browser automation host is connected.`; durable-operation browser choice/consume/resume/revalidation and packed product-choice certification remain open. | **SLICE_BLOCKED — Campaign Director disposition 2026-09-25.** Production lease wiring and deterministic packed lifecycle recovery pass; durable-operation browser choice/consume/resume/revalidation remains open because Paseo reported no connected browser automation host. REAL_PROVIDER lifecycle certification remains a separate open lane. |

| AEH-V2-0046 | P1 | Detached operation bootstrap failure finalization | A non-Git project operation was persisted as `RUNNING/preparing` and exited with a dead controller PID after strict `baseRef=main` resolution failed; no terminal event or current failure projection was written. | HEAD and the Lead-selected packed release performed config load, base-ref resolution, and portfolio sync after the RUNNING transition but before the failure-finalization catch. The exception escaped terminalization. | `src/operations/controller.ts`, `tests/operationStartupFailure.test.ts`, `docs/CORE_ARCHITECTURE_V2_S9_WORKGRAPH.md`, S9 status/conformance documents | The single regression exercises `executeOperation` and production state APIs in an initialized non-Git root. It asserts terminal `FAILED`, diagnostic, terminal event, current portfolio projection, and no participants/process handles/provider leases. Flash baseline run reproduced the escape; Luna rerun passes against the fix. | Moved the RUNNING transition and bootstrap steps inside the existing controller try/catch, preserving strict base-ref failure and controller fencing. The controller does not synthesize a Git base. | Flash: `npx vitest run tests/operationStartupFailure.test.ts` against `c036471` — exit 1, 1 failed at `resolveBaseRef` before catch. Integrated current source: `./node_modules/.bin/vitest run tests/operationStartupFailure.test.ts --reporter=dot` — 1 file / 1 test passed; `npm run typecheck` — exit 0. Director independently ran the earlier draft — 1 test passed, typecheck and build exit 0 (`release-1790359341925-17-d65e1b0f`). Fresh package SHA-256 `77a0c39f580053b67abe2ee444e6b4c2995ecdc417a66613eb56d5dc20a1782d`, selected release `release-1790359422074-34-41c17389`; packed inspection confirms the catch encloses RUNNING/config/base-ref/portfolio bootstrap. | Director owns recovery of the same durable operation after ordinary Git initialization. Real Playwright choice/consume/resume/replay/cancel/refreshed-projection evidence remains pending; no product browser acceptance is claimed. | FIXED_AND_DETERMINISTICALLY_VERIFIED (S9 R12 subgate; full S9/browser gate open) |

| AEH-V2-0047 | P1 | Operation Supervisor provider identity | The canonical Operation Supervisor could not acquire a provider lease or bind a structured result: `PASEO_PROVIDER_LEASE_PARTICIPANT_UNKNOWN`, `V2_RESULT_PROVENANCE: execution binding participant is not registered for this role`, and `CONTEXT_RUNTIME_V2_BINDING_REJECTED`. | Supervisors are tracked in `supervision.generations` and deliberately excluded from `operation.participants`, but the S9 provider-lease, result-provenance, context-authorization, and dispatch paths all resolved actor identity from the participant map only. `dispatchMaterializedAgentPrompt` also used the provider session id as the authority participant and omitted the projected prompt and bound actor labels. | `src/runtime/supervisorV2.ts`, `src/runtime/providerLifecycle.ts`, `src/paseo/runtimeCore.ts`, `src/operations/state.ts`, `src/workers/agentPrompt.ts`, `src/workers/resultGateway.ts`, `src/context/authorizationV2.ts`, `tests/paseoProviderLeaseLifecycle.test.ts`, `tests/managedLeadAcceptance.test.ts`, `tests/changeControllerOwnedDecision.test.ts` | Deterministic regressions cover supervisor lease identity, agent-record execution binding, prompt-manifest dispatch, and the full controller-owned FORMAL_SDD journey. | A supervisor actor (`supervisorAgentId`) is validated against the durable agent record for leases, execution bindings, context authorization, and result provenance; dispatch uses the materialized participant identity, the projected prompt, and complete bound labels. | Focused S9 set 25 files / 221 tests passed; `tests/changeControllerOwnedDecision.test.ts` passed; typecheck/build exited 0. | Real browser journey passed 46/46; supervisor initialization ran through the scripted provider boundary (not REAL_PROVIDER). | FIXED_AND_DETERMINISTICALLY_VERIFIED (S9 completion; independent review pending) |
| AEH-V2-0048 | P1 | Agent preset vs frozen RoleProfile ceilings | FORMAL_SDD failed at the first Explorer turn with `V2_AUTHORITY_DENIED: role 'Explorer' cannot receive capabilities outside its compiled RoleProfile ceiling: execute`; lead, librarian, planner, spec-manager, and reviewer presets had the same class of mismatch, and preset skill lists referenced repository skills absent from the seed catalog (`SKILL_MANIFEST_INVALID`). | The default preset predated the frozen role ceilings and granted shell/network/write/delegate capabilities the ceilings deny, plus repository skill ids the SkillManifest compiler cannot resolve. The effective authority was already the ceiling, so the excess only produced hard launch failures. | `presets/agents/default.jsonc`, `presets/agents/orchestration.jsonc` | The controller-owned FORMAL_SDD journey reaches the Spec Manager and suspends; topology/hygiene tests still pass. | Aligned each default-preset permission set to the compiled ceiling and replaced unresolvable repository skill ids with seeded skills. Repository-skill resolution into SkillManifest remains an open capability gap. | Focused S9 set passed; `tests/changeControllerOwnedDecision.test.ts` passed; typecheck/build exited 0. | Real browser journey passed 46/46. | FIXED_AND_DETERMINISTICALLY_VERIFIED (repository-skill resolution gap recorded) |
| AEH-V2-0049 | P1 | Multi-choice Spec Manager continuation | A second product-choice suspension in the same FORMAL_SDD Spec Manager loop failed with `DECISION_REQUEST_STATE_INVALID` because the guard required phase `spec-authoring` while the resumed turn had advanced to phase `context`. | `suspendOperationForProductChoice` computed `replacingConsumedChoice` with an over-strict phase equality instead of the durable `RESUMING` continuation state. | `src/operations/state.ts`, `tests/changeControllerOwnedDecision.test.ts` | The regression drives decision, consumption, resume, and a second suspension through the real controller. | The guard now accepts a consumed `RESUMING` continuation without a pending request. | `tests/changeControllerOwnedDecision.test.ts` passed; focused S9 set passed. | Real browser journey exercised two controller-owned suspensions and passed 46/46. | FIXED_AND_DETERMINISTICALLY_VERIFIED |
| AEH-V2-0050 | P1 | Cancellation writer fencing | Cancellation could not terminalize because it attempted `paseo stop` on authority participant identities (`participant:<digest>`) and scripted fixture sessions, producing `AEH_CANCELLATION_FENCING_REQUIRED`. | The cleanup loop treated every registered agent id as a provider session; authority identities and deterministic fixture sessions have no external process. | `src/operations/controller.ts`, `src/paseo/deterministicRuntime.ts` | `tests/operationLifecycleRegression.test.ts` still stops real registered agents; the controller-owned journey now cancels to `CANCELLED`. | Authority `participant:<16hex>` ids are skipped, deterministic session ids are recognized by prefix in any lifecycle owner, and deterministic provider leases are released without an external stop. | Focused S9 set passed; `tests/changeControllerOwnedDecision.test.ts` terminalizes CANCELLED. | Real browser journey cancelled to durable `CANCELLED` with stale-writer and stale-decision rejection; 46/46 passed. | FIXED_AND_DETERMINISTICALLY_VERIFIED |
| AEH-V2-0051 | P2 | Operation coordination lock and candidate task id | Supervisor initialization timed out acquiring its own coordination lock (`Timed out acquiring operation state lock ...coordination`), and a CHANGE operation with an explicit `--task` id failed structured-result provenance with `AEH_RESULT_PROVENANCE: task identity does not match the candidate`. | `withOperationCoordinationLock` was not re-entrant for the holding async context, and `saveOperation` always bound the operation id as the change candidate task id. | `src/operations/state.ts`, `tests/operationStartupFailure.test.ts`, `tests/changeControllerOwnedDecision.test.ts` | The controller-owned journey exercises nested context authorization and an explicit task id. | The coordination lock is re-entrant only within the holding async context (other callers still serialize on the file lock); the change candidate task id uses the payload task id when declared. | Focused S9 set passed; typecheck/build exited 0. | Real browser journey passed 46/46. | FIXED_AND_DETERMINISTICALLY_VERIFIED |
| AEH-V2-0052 | P1 | Rootless sandbox and validator isolation | Rootless/network/scope isolation and validator-command isolation were unproven: the only hardened worker path built Podman arguments, `podman`/`buildah` were absent, isolation was asserted only by argument-array unit tests, and validator commands executed as host child processes through `runShell` with no namespace boundary. | No execution provider abstracted rootless isolation; `src/security/sandbox.ts` produced Podman flags and `src/workers/agentPrompt.ts` spawned `podman run`, while `src/validators/*` ran commands directly on the host. | src/security/isolation.ts, src/validators/external.ts, src/validators/toolCommand.ts, src/validators/commands.ts, src/validators/registry.ts, src/core/verify.ts, src/core/run.ts, src/core/types.ts, src/core/config.ts, schemas/project.schema.json, src/utils/process.ts, tests/security/isolation.test.ts, tests/security/validatorIsolationFailClosed.test.ts, tests/security/providerFailClosed.test.ts, tests/system/aehIsolationCampaign.test.ts | Real rootless `bwrap 0.13.0` execution proves user/mount/PID/UTS/IPC/network namespaces, network denial by real `ENETUNREACH`, host-home/PID/env masking, workspace-only writes, read-only `/etc`, and validator repository-mutation/egress denial; missing or unsupported providers return explicit `ISOLATION_PROVIDER_UNAVAILABLE`/`ISOLATION_PROVIDER_UNSUPPORTED` blockers without running the validator. Focused suites 5 files / 30 tests passed; system lanes scenario+concurrency+isolation 3 files / 20 tests passed. | Real local OSS provider execution (bwrap) with real kernel namespaces; rootless Podman/OCI execution remains unprovisioned and fails closed. | FIXED_AND_REAL_LOCAL_PROVIDER_VERIFIED (S10; independent slice review pending) |
| AEH-V2-0053 | P1 | Candidate-bound SAST evidence | SAST was not wired: `ValidationContext` had no candidate identity, the external tool validator wrote only a `spec.id`-named raw artifact, and no SAST report was traceable to the exact current CandidateRevision. | Security validation ran without a candidate binding, and evidence artifacts were not candidate-scoped or digest-verified. | src/security/sastEvidence.ts, src/validators/external.ts, src/validators/types.ts, src/core/run.ts, tests/security/sastEvidence.test.ts, tests/packed/s10RealSastCampaign.mjs, docs/evidence/s10/real-sast-campaign.json | `SastEvidenceV1` binds candidate identity, workspace source digest, adapter, tool version, command digest, raw-output digest, findings, and isolation evidence under a candidate-scoped path; digest/tamper/stale/missing checks block; the S4 security impact path requires it for opengrep/trivy. Real local OSS Trivy v0.70.0 produced a finding-bound FAIL for r1 and a clean PASS for r2, with r1 evidence rejected as stale for r2. | Real local OSS scanner execution; hosted-provider certification remains S13. | FIXED_AND_REAL_LOCAL_PROVIDER_VERIFIED (S10; independent slice review pending) |

## Audit-snapshot reconciliation

This section corrects claims that the read-only architecture audit found stale or overbroad at commit f1518caf405f44851227cf73ea37b79267b1a098. It is a reconciliation note, not a roadmap. Current subsystem state, dependencies, and next gates are maintained in [Core Architecture v2 Status](CORE_ARCHITECTURE_V2_STATUS.md); evidence-lane boundaries are maintained in [Core Architecture v2 Conformance](CORE_ARCHITECTURE_V2_CONFORMANCE.md).

- **Repairer public path:** the earlier claim that candidate-mutation helpers had no production callers and that the live repair path mutated in place is superseded. Repairer candidate mutation and assembly support have production use. Remaining gaps concern complete frozen Repairer assignment and broader operation-state, impact, validation, review, and acceptance integration.
- **StructuredResult provenance:** candidate, generation, and epoch checks exist; blueprint and policy fields are optional in some paths. Production launchers still omit required full identity digests. Provenance is incomplete, not wholly absent or complete.
- **ExecutionCatalog:** the catalog exists and is used. Its policy and tool-authorization fields are not fully enforced. Do not describe the catalog itself as absent.
- **Operation and role policy:** ResolvedOperationPolicy and RoleInvocationPolicy are absent in the audited snapshot. They are distinct from ExecutionCatalog, which exists with incomplete policy/tool enforcement.
- **Semantic assessment:** typed assessment contracts and helpers exist, but the audited public paths do not construct the service consistently. Public semantic wiring remains open.
- **Paseo session binding:** a digest-bound binding module and tests exist, but the production Paseo runtime does not call it.
- **CandidateImpact:** the finding that impact did not recompile reviewer, validation, and acceptance requirements describes the audited snapshot and is superseded by S4 / AEH-V2-0041. S6 now implements the production AcceptanceOracle and objective-completion path; its deterministic gate was accepted by the Campaign Director on 2026-09-24. External certification remains separate.
- **Human decisions and cancellation (S3 remediation, 2026-09-24):** V2 HumanDecisions are consumed for exact policy-required action authorization, current-scope cancellation, and paired-session product choice. The Spec Manager BLOCKED boundary creates a typed, evidence-bound DecisionRequest and durable continuation/checkpoint; the Control Center App submits only the offered choice identity, and the controller consumes it once, revalidates current bindings, resumes SPEC_AUTHORING, and invalidates dependent execution semantics when requirements change. Campaign Director review found that consumed-choice recovery and checkpoint loading did not compare the complete saved/current binding. The Luna-owned follow-up now persists the original selected-decision binding, checks it against the ledger receipt and current operation, validates checkpoint bindings against both continuation and current operation, permits only the single authorized requirement revision under the same candidate/epoch, and never reuses a stale consumed choice. After takeover it validates the old receipt and unchanged candidate/revision, then opens a fresh current-binding HUMAN_REQUIRED request; intact unanswered requests can be safely reissued after policy/epoch drift only with unchanged candidate and execution revision. The bounded crash window after semantics persistence is recovered by the controller’s bootstrap policy binder before continuation load; the regression proves missing policy fails the load, bootstrap binds a current deterministic policy, and current checkpoint/resume validation then succeeds. Deterministic regressions cover revision, policy, epoch, digest-recomputed checkpoint tampering, same-epoch crash recovery, and takeover reconfirmation. Controller-owned mutations and active terminal transitions require current epoch plus controller token, while Lead acknowledgement binds the current Lead actor, epoch, and operation revision. Cancellation establishes and rechecks its fence before cleanup. Final S3 deterministic evidence is recorded in Conformance; browser, live OPA, real-provider, and packed adversarial/provider-fault certification remain separate pending lanes.
- **Acceptance and delivery:** validation, review/quality, and AcceptanceOracle are distinct. S6 persists a current controller-owned disposition and enforces it before delivery; where frozen policy requires managed Lead evidence, that evidence is collected before the disposition. S8 was independently accepted by the Campaign Director; external-provider/packed journey certification remains separate and pending.
- **Handoff:** the audited stale CLI path and pre-acceptance public auto-handoff finding are superseded by S8: normal issue execution enters the managed controller path, accepted finalization owns requested public effects, and standalone `sdd handoff` fails explicitly before project loading because it lacks managed acceptance context. Uncertain Paseo workspace identity still fails closed as HUMAN_REQUIRED. Deterministic fixtures do not establish live-provider behavior or complete packed reconciliation; the Campaign Director accepted S8 on its deterministic gate on 2026-09-25.
- **Packed campaign wording:** some campaign tests import src/** directly. Earlier descriptions of the campaign as entirely packed are overbroad; a complete freshly packed product journey is not established.
- **Serena evidence:** the recorded Serena smoke covers the tested MCP broker path only. It does not certify unused provider lease/release lifecycle or real-model execution.
- **Validation counts:** dated command totals below and in findings are historical checkpoint evidence. They are not results rerun during the f1518ca audit or this S0 work, and they are not current certification claims.
- **Delivery claim:** the PR body in src/delivery/finalize.ts says the lead-acceptance workflow completed before PR creation. S6 now enforces the current AcceptanceOracle disposition before effects and collects managed Lead evidence first when policy requires it; the PR wording remains policy-conditional and does not establish Campaign Director acceptance or external-provider certification.
- **Working-tree note:** the historical campaign note about uncommitted implementation/test changes described an earlier point in the campaign. At the f1518caf405f44851227cf73ea37b79267b1a098 audit snapshot, git status was clean. The fsmonitor warning did not prevent branch status from being read.

## Historical campaign notes

The following items preserve campaign history and must not be read as current certification or roadmap:

- The branch campaign was based on the repository state described in the recorded findings. No history rewrite or destructive Git recovery was performed.
- The accidental operation AUDIT-20260922T221454Z-7aa6fa6b was cancelled after inspection and reached terminal FAILED; its workspace was the development checkout and it created no additional Git worktree.
- A historical integrator checkpoint recorded an aggregate isolated packed system campaign and a packed Home/Control Center smoke. The cited campaign included tests that imported source directly; these records do not prove all-packed journeys or current certification.
- Historical environment notes reported no Paseo executable, browser automation executable/browser, or real model/provider journey at that checkpoint. Serena's local MCP broker smoke was provider-process evidence for that path, not model certification.
- AEH workflows are not used to govern changes to this development checkout. The S0 documentation work did not run AEH.
