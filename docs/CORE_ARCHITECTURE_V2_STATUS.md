# Core Architecture v2 Status

**Role:** Living roadmap and current-state evidence map. The normative target is [Core Architecture v2](CORE_ARCHITECTURE_V2.md). Conformance lane boundaries are in [Core Architecture v2 Conformance](CORE_ARCHITECTURE_V2_CONFORMANCE.md); historical incidents and findings remain in the [Engineering Ledger](ENGINEERING_LEDGER.md).

## Evidence and verification boundary

The baseline current-state evidence came from the read-only architecture audit at commit f1518caf405f44851227cf73ea37b79267b1a098. S1–S8 have been accepted on their deterministic slice gates; the Campaign Director accepted S8 on 2026-09-25. S7/S8 deterministic results do not certify live providers, browsers, OPA, adversarial behavior, or packed end-to-end journeys. On 2026-09-25, S10 exercised a real rootless bwrap isolation provider and a real local OSS Trivy v0.70.0 scanner; those are local real-provider executions and do not certify hosted providers, rootless Podman/OCI, OPA, packed journeys, or S13 provider certification.

EVIDENCE LANE names the kind of evidence cited in the audit, not a claim that a test or provider was rerun during S0. REAL_PROVIDER, BROWSER, ADVERSARIAL, and PACKED_E2E evidence remain separate from CONTRACT, UNIT, and INTEGRATION evidence. Historic test counts are checkpoint records, not current certification.

## Stable subsystem roadmap

| Subsystem | TARGET | CURRENT | STATUS | GAP | DEPENDENCIES | NEXT GATE | EVIDENCE | EVIDENCE LANE | LAST VERIFIED |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Candidate truth and assembly | Immutable CandidateRevisions; controller-owned assembly; truthful bases and lineage. | Assembly, wave lineage, repair, and workspace identity checks are wired. S1 candidate assembly invalidates bindings and structured results for the prior candidate. S4 impact and assurance use the exact assembled CandidateRevision; S6 acceptance and objective-completion evidence bind that current candidate. S7 provenance binds the same candidate. S8 controller bootstrap provisions an isolated internal execution workspace before acceptance while binding it to the current candidate; the delivery branch is separate from the local candidate branch. | S7 deterministic gate accepted 2026-09-24; S8 deterministic gate accepted by Campaign Director 2026-09-25. | No known deterministic S8 candidate-identity gap. Real-provider workspace and packed-candidate behavior remain separate evidence lanes. | S1 current CandidateRevision and BuildIdentity; S6 AcceptanceOracle before effects; frozen supply-chain policy. | Preserve current CandidateRevision and BuildIdentity; keep provider and packed certification pending. | src/candidates/assembler.ts, src/core/run.ts, src/architecture/candidateAssurance.ts, src/provenance/generate.ts, src/operations/controller.ts, src/delivery/finalize.ts; tests/candidateIdentity.test.ts, tests/deliveryFinalize.test.ts, tests/security/toolActionGate.test.ts. | CONTRACT; UNIT; INTEGRATION; BUILD; SYSTEM_DETERMINISTIC; REAL_PROVIDER/BROWSER/OPA_PROVIDER/ADVERSARIAL/PACKED_E2E pending | S8 remediation evidence is recorded below. |
| Authority kernel | Frozen policy, role ceilings, leases, epoch fencing, gated actions, and consumed scoped HumanDecisions. | Controller mutations retain current epoch, controller token, candidate and frozen-policy checks. ToolActionGate classifies paseo.workspace.create as LOCAL_RESOURCE_CREATION and excludes it from delivery external-effect policy; the fenced controller still persists intent/receipt and unresolved workspace identity remains HUMAN_REQUIRED. Public GitHub, push and PR actions remain current-acceptance gated. | S3 and S8 deterministic authority/action gates accepted; S8 by Campaign Director 2026-09-25. | No known deterministic authority/action gap from S8. Live provider identity inspection and external certification remain pending. | Candidate identity; S1 execution identity; current policy and epoch. | Preserve S3 authority/fencing and accepted S8 controls; retain separate provider, browser, OPA and packed evidence lanes. | src/security/actionPolicy.ts, src/security/toolActionGate.ts, src/security/gatedAction.ts, src/security/actionReconciliation.ts, src/operations/controller.ts; tests/security/actionPolicy.test.ts, tests/security/toolActionGate.test.ts, tests/security/gatedAction.test.ts, tests/security/actionReconciliation.test.ts. | CONTRACT; UNIT; INTEGRATION; BUILD; SYSTEM_DETERMINISTIC; REAL_PROVIDER/BROWSER/OPA_PROVIDER/ADVERSARIAL/PACKED_E2E pending | S8 remediation evidence is recorded below. |
| Intent, route, assurance, and ResolvedOperationPolicy | Semantic intent and route constrained by deterministic policy; independent assurance minimum; frozen policy digest. | S1 persists the immutable ResolvedOperationPolicy before participant binding. Non-explicit intent and route use typed Semantic Assessor judgments through the AgentTopology-resolved Paseo runtime; explicit user intent remains DETERMINISTIC. INTENT admits HIGH-risk requests within existing assessment ceilings; deterministic high-risk routing and CRITICAL assurance floors remain unchanged. S3 consumes frozen policy identity at sensitive-action, product-choice, cancellation, and decision boundaries without changing S2 intent or route semantics. | IMPLEMENTED | REAL_PROVIDER execution is not established by deterministic fakes. | S1 identity; AgentTopology and semantic capability policy; Paseo runtime; S3 authority kernel. | Preserve S1/S2 policy and deterministic floors; certify provider behavior separately. | src/entry.ts, src/audit/intent.ts, src/core/triage.ts, src/cli.ts, src/operations/change.ts, src/core/contract.ts, src/semantic/assessment.ts, src/security/toolActionGate.ts; tests/auditIntent.test.ts, tests/triage.test.ts, tests/semanticAssessment.test.ts, tests/security/toolActionGate.test.ts. | CONTRACT; UNIT; REAL_PROVIDER pending | 2026-09-24 S3 policy-consumption regression verification; S2 semantics unchanged |
| Semantic decision runtime | Production semantic decisions use bounded evidence-bound model assessment with deterministic post-checks. | SemanticAssessmentServiceV1 resolves one AEH Semantic Assessor from AgentTopology and semantic capability policy, executes the configured agent through Paseo, validates typed output, policy, binding, receipt, cache, and actual session provenance, and stores SemanticAssessmentV1. INTENT, ROUTE, STACK, ISSUE, FAILURE, CANDIDATE_IMPACT, and VALIDATION_NEED are supported. Semantic Assessor is an AEH Agent and is rejected from WorkGraph Participant invocation. STACK additionally requires canonical-root and repository-content binding; its generic inventory is aligned with the Git digest file set and currentness is checked before/after capture and after assessment. | IMPLEMENTED | Deterministic Paseo fakes verify contracts but do not establish REAL_PROVIDER behavior. | Accepted S1 identity interfaces; AgentTopology; Paseo; deterministic semantic capability policy. | Keep provider certification separate; reject stale topology, session, evidence, policy, and cache provenance. | src/semantic/assessment.ts, src/semantic/runtime.ts, src/agents/permissions.ts, src/agents/outputContracts.ts, src/workers/agentPrompt.ts; tests/semanticAssessment.test.ts, tests/semanticAssessmentRuntime.test.ts, tests/semanticAssessorTopology.test.ts, tests/participantsStackDiscovery.test.ts. | CONTRACT; UNIT; REAL_PROVIDER pending | 2026-09-24 S2 boundary remediation deterministic verification |
| Stack discovery and semantic stack profile | HYBRID: model interprets bounded repository/candidate evidence; deterministic code verifies identity, read boundary, receipts, profile schema, provenance, policy, and cache freshness. | runTask supplies current bound repository/candidate evidence to discoverProjectStackProfile; profile output is MODEL-only, preserves unknowns, and is passed to delegated participant planning and validation script resolution. STACK requires `repositoryRootDigest` for the canonical realpath and matches `repositoryDigest` before scan, after evidence capture, after the assessor returns, and before profile return. Evidence paths come from the Git-tracked and non-ignored untracked inventory used by the digest, or a stricter generic subset; ignored `.env` and `.harness` runtime/cache paths are absent. Model-proposed projectSkillRoots are preserved only after normalized relative-path, existence, and canonical-root containment checks and grant no skills/capabilities. Technology-specific deterministic recognition and detector APIs are removed. | IMPLEMENTED | Repository evidence limits may produce explicit unknowns. Real model interpretation is not certified by fixture tests. | Semantic decision runtime; candidate identity; authorized repository boundary. | Keep the generic non-ignored read inventory, root/content checks, and stale-result rejection; actual provider evidence remains a separate lane. | src/participants/stack.ts, src/participants/index.ts, src/core/run.ts, src/core/git.ts, src/semantic/runtime.ts, src/agents/waveExecutor.ts, src/architecture/validationRequirements.ts; tests/participantsStackDiscovery.test.ts, tests/participantsCanonical.test.ts, tests/semanticAssessmentRuntime.test.ts. | CONTRACT; UNIT; REAL_PROVIDER pending | 2026-09-24 S2 repository evidence/path remediation deterministic verification |
| Issue intake | HYBRID: typed ISSUE interpretation plus canonical Planner authoring and deterministic source/scope/policy checks. | The public issue implement/run path prepares the existing issue, then starts a managed controller run. Controller-owned disposable workspace setup remains pre-acceptance; default auto-handoff no longer creates a public branch before acceptance. Accepted finalization gates and receipts any missing issue/branch and configured push/PR effects. The standalone `aeh sdd handoff` command reports `SDD_HANDOFF_UNSUPPORTED` before project loading instead of entering delivery without managed context. | S2 deterministic gate accepted; S8 issue-flow and CLI-surface remediation accepted by Campaign Director 2026-09-25. | No known deterministic issue-flow gap in the managed path. Standalone SDD handoff is intentionally unsupported; live GitHub/Paseo execution is not certified by fixtures. | Semantic decision runtime; immutable issue snapshot; canonical Planner; route policy. | Preserve the managed issue path and acceptance boundary; keep live provider and packed issue journeys pending. | src/issues/intake.ts, src/issues/workflow.ts, src/cli.ts, src/delivery/handoff.ts, src/delivery/finalize.ts; tests/issueIntake.test.ts, tests/issueDeliveryIdentity.test.ts, tests/deliveryFinalize.test.ts, tests/cliS8Handoff.test.ts. | CONTRACT; UNIT; INTEGRATION; BUILD; REAL_PROVIDER/PACKED_E2E pending | S8 remediation evidence is recorded below. |
| WorkGraph, scheduling, and resource claims | Model proposes decomposition; deterministic validation/scheduler enforces dependencies, scope, claims, and isolation. | WorkGraph, claims, topological waves, parallelism, and scope checks remain deterministic. Typed semantic ROUTE and issue-route decisions feed contract routing and the bounded Planner/WorkGraph path; stack profile is supplied to ParticipantPlan compilation and validation resolution. S4 selects only a final-wave CandidateImpact whose candidate identity exactly equals the operation's current CandidateRevision; missing or stale impact blocks the assurance gate. S6 terminal success also checks that every required WorkUnit is accounted for in the complete current objective evidence set. | IMPLEMENTED | Live semantic-provider behavior and packed operation journeys remain separate pending lanes; S4 alone does not make validation or review an AcceptanceOracle. S6 deterministic implementation was accepted by the Campaign Director on 2026-09-24; provider and packed evidence remain separate pending lanes. | Route/policy; candidate truth; semantic decision runtime; S4 assurance compiler. | Preserve deterministic claims and current candidate bases; require accounted WorkUnits and the current AcceptanceOracle/objective-completion checks for terminal success. | src/architecture/workGraph.ts, src/agents/parallelism.ts, src/agents/waveExecutor.ts, src/core/run.ts, src/issues/intake.ts; tests/architectureWorkGraph.test.ts, tests/parallelism.test.ts, tests/triage.test.ts, tests/candidateWaveLineage.test.ts. | CONTRACT; UNIT; INTEGRATION; REAL_PROVIDER pending | 2026-09-24 S4 focused 7 files / 41 tests; full non-system 153 files passed, 1 skipped (848 passed, 7 skipped); typecheck/build passed. |
| Participant and role compiler | Deterministically compile minimal ParticipantPlan, canonical roles, specializations, skills, ToolPacks, scope, and output contracts. | S1 role ceilings and per-participant bindings remain unchanged. Semantic Assessor is included only in the Agent role space and cannot be selected as a Participant; stack profile specializes bounded planner assignments without granting new tools or authority. | PARTIAL | Repairer selection and complete catalog/tool-policy consumption remain open. | WorkGraph; policy; KnowledgeGate; Semantic Assessor remains outside Participant plans. | Retain deterministic S1 role ceilings and Agent/Participant distinction. | src/architecture/participantPlan.ts, src/participants/types.ts, src/agents/types.ts, src/agents/config.ts, src/workers/agentPrompt.ts; tests/semanticAssessorTopology.test.ts, tests/architectureWorkGraph.test.ts. | CONTRACT; UNIT | 2026-09-24 S2 deterministic verification |
| Knowledge, Librarian, and skill trust | Version-aware knowledge resolution; read-only Librarian; deterministic trust; operation-local skill procedures delivered and identity-bound. | S1 retains accepted ephemeral procedure text and per-step claim/source grounding in a scoped SkillManifest, projects it verbatim only to the authorized assignee, and binds its digest through launch and result provenance. The procedure-text mutation rejection and trust-decision-digest mutation rejection both remain asserted. | PARTIAL | Provider-backed Librarian evidence and full freshness/source-policy lifecycle remain unverified; no real-provider claim is made. | Participant compiler; S1 SkillManifest identity; S5 context runtime. | Keep trust deterministic and preserve the exact S1 manifest/projection boundary; provider evidence remains a separate lane. | src/knowledge/index.ts, src/architecture/executionIdentity.ts, src/workers/agentPrompt.ts; tests/knowledgeGate.test.ts, tests/agentPromptStructuredResultProvenance.test.ts, tests/distributedExecutionIdentity.test.ts. | CONTRACT; UNIT; REAL_PROVIDER pending | 2026-09-24 S1 remediation deterministic verification |
| Context Runtime and progressive exposure | JIT, authorized, budgeted, source-provenanced ContextManifest; verbatim normative fragments; exact prompt digest. | S5 wires strict controller-only ContextRefAuthorizationV1 issuance from current S3 owner state and a current participant read lease. Grants bind operation/revision, project, candidate, participant/generation, blueprint, policy, epoch, actual session, exact refs/source digests, expiry, and retrieval budget. Retrieval rechecks the current binding, envelope, source bytes, session, expiry, request uniqueness, and durable limits; continuations bind the current binding, references, and receipts and reject stale/replayed state. S1 launch ContextManifest/PromptManifest digests remain immutable and authoritative; StructuredResult adds a ProgressiveContextManifestV1 digest over the exact receipt set, delivered-byte digests, used budget, state, and launch digests. Planner/Explorer handoffs use their existing deterministic projections; normative and accepted skill content remain verbatim. At grant issuance, the controller verifies the immutable launch ContextManifest digest against the current ExecutionBinding, its envelopeDigest against the verified durable envelope provenance digest, and exact unique (refId, artifactPath, sourceDigest) set equality before constructing the grant. | DETERMINISTICALLY_VERIFIED (S5 gate accepted) | No known S5 deterministic gate gap. REAL_PROVIDER behavior for live Paseo/MCP context retrieval remains unverified. | Accepted S1 identity; accepted S3 owner/epoch and lease semantics. | Preserve the frozen grant and receipt checks; provider-backed context and session behavior remain separate evidence lanes. | src/context/authorizationV2.ts, src/context/runtimeV2.ts, src/context/retrieval/, src/context/envelope.ts, src/context/gateway.ts, src/workers/agentPrompt.ts; tests/contextAuthorizationV2.test.ts, tests/contextRuntimeV2.test.ts, tests/contextEfficiency.test.ts, tests/agentPromptStructuredResultProvenance.test.ts. | CONTRACT; UNIT; INTEGRATION; BUILD; SYSTEM_DETERMINISTIC; REAL_PROVIDER pending | 2026-09-24 S5 remediation red/green 1 file (4 failed / 4 passed before repair; 8 passed after); focused context/session/provenance 12 files / 125 tests; full non-system 152 files passed, 1 skipped (835 passed, 7 skipped); independent Campaign Director rerun: focused 12 files / 125 tests, TypeScript exit 0, build exit 0 (`release-1790294205611-17-b4fe25cb`), relevant deterministic system suites 3 files / 45 tests; REAL_PROVIDER pending. |
| ExecutionBlueprint and RoleInvocation identity | Complete static blueprint and per-launch binding without granting authority. | Versioned ExecutionBlueprint V2 and S1 ExecutionBinding V2 remain authoritative. S5 issues addressable context only after checking the exact current participant binding, candidate, policy, controller epoch, actual session, and an existing read lease; context cannot add or widen lease/capability authority. The prepared launch prompt and context digests remain those bound in ExecutionBinding. Before grant construction, the issuer also checks the exact bound launch-manifest digest, envelope provenance digest, and full addressable-ref tuple set. | DETERMINISTICALLY_VERIFIED (S1 contract plus S5 context/session lifecycle) | No known S5 deterministic identity gap; REAL_PROVIDER execution remains unverified. | Policy; ParticipantPlan; SkillManifest; accepted S3 owner and lease contracts. | Preserve lifecycle-owned binding transitions and keep provider behavior in a separate evidence lane. | src/architecture/executionIdentity.ts, src/security/executionLease.ts, src/context/authorizationV2.ts, src/workers/agentPrompt.ts; tests/contextAuthorizationV2.test.ts, tests/agentPromptStructuredResultProvenance.test.ts, tests/security/controllerFencing.test.ts. | CONTRACT; UNIT; INTEGRATION; REAL_PROVIDER pending | 2026-09-24 S5 remediation verification: context/session/provenance 12 files / 125 tests; full non-system 152 files passed, 1 skipped (835 passed, 7 skipped); TypeScript/build passed; relevant deterministic system suites 3 files / 45 tests passed. |
| StructuredResult provenance | Every result binds current candidate, operation execution revision, generation, epoch, blueprint, policy, context/prompt, skills, runtime, and contract. | S1 full binding checks remain in force. S5 closes retrieval at result acceptance and persists ContextRetrievalEvidenceV1 with exact current binding and launch-manifest digests, durable receipt IDs/digests, delivered-byte digests, budget/use, and a domain-separated ProgressiveContextManifestV1 digest. Result reload verifies this evidence against durable receipts and rejects stale or altered evidence. The controller only issues the launch grant after exact ContextManifest/envelope evidence equality has been established. | DETERMINISTICALLY_VERIFIED (S1 adapter plus S5 context evidence) | Deterministic adapters do not certify live provider/MCP behavior; no known S5 deterministic evidence gap. | S1 execution identity; S3 controller fencing; S5 current context authorization. | Preserve full provenance and receipt closure; real transport/provider behavior remains a separate evidence gate. | src/workers/resultGateway.ts, src/context/authorizationV2.ts, src/workers/agentPrompt.ts; tests/structuredResultGateway.test.ts, tests/agentPromptStructuredResultProvenance.test.ts, tests/contextAuthorizationV2.test.ts, tests/distributedExecutionIdentity.test.ts. | CONTRACT; UNIT; INTEGRATION; REAL_PROVIDER pending | 2026-09-24 S5 remediation verification: context/session/provenance 12 files / 125 tests; full non-system 152 files passed, 1 skipped (835 passed, 7 skipped); TypeScript/build passed; relevant deterministic system suites 3 files / 45 tests passed. |
| Paseo session identity | Reuse requires exact durable operation/participant/candidate/blueprint/policy/context binding. | PaseoSessionBindingV1 now has strict full canonical identity including operation execution revision, participant generation, candidate, blueprint, policy, context/prompt digests, and controller epoch. Worker prompt preparation durably binds the actual provider session; only ACTIVE exact matches are reusable. A different actual session rotates the durable binding; same-session semantic drift rejects reuse until a new session is materialized. StructuredResult still names the actual returned session. | DETERMINISTICALLY_VERIFIED (S5 binding and lifecycle) | No known S5 deterministic gate gap. REAL_PROVIDER service behavior is not certified by fakes. | S1 ExecutionBinding; S5 context manifests; accepted S3 epoch fencing. | Keep exact equality at every reuse boundary; provider-backed session behavior remains separate evidence. | src/paseo/sessionBinding.ts, src/workers/agentPrompt.ts, src/paseo/launchSpec.ts, src/workers/resultGateway.ts; tests/paseo/sessionBinding.test.ts, tests/agentPromptStructuredResultProvenance.test.ts, tests/paseoPendingExecutionIdentity.test.ts, tests/paseoInitialTurnBarrier.test.ts. | CONTRACT; UNIT; INTEGRATION; REAL_PROVIDER pending | 2026-09-24 S5 remediation verification: session/context/provenance 12 files / 125 tests; full non-system 152 files passed, 1 skipped (835 passed, 7 skipped); TypeScript/build passed; relevant deterministic system suites 3 files / 45 tests passed. |
| CandidateImpact | Assess the assembled candidate semantically and deterministically with typed impact dimensions. | Every production CandidateAssembler path in runTask, wave integration, and repair assesses bounded post-assembly file/patch receipts and emits candidate, base-candidate, patch, changed-path, semantic digest, and impact digest bindings. runTask consumes the exact current impact and recompiles the pure deterministic assurance plan under the current frozen policy. S6 consumes the resulting assertions/evidence-strength without reinterpreting impact. Invalid, absent, stale, or tampered impact fails closed. | DETERMINISTICALLY_VERIFIED (S4 deterministic gate; provider pending) | MODEL impact interpretation and live provider/session behavior are not certified by deterministic fixtures. S6 does not widen or rewrite the S4 impact/assertion/evidence-strength interface. | Candidate truth; accepted S2 semantic runtime; S3 policy/authority; S5 context/session identity. | Preserve post-assembly binding and fail-closed behavior; acceptance consumes only assertions compiled for the exact current impact and candidate. | src/candidates/assembler.ts, src/candidates/wave.ts, src/candidates/repair.ts, src/core/run.ts, src/architecture/candidateAssurance.ts, src/architecture/acceptanceOracle.ts; tests/candidateAssembler.test.ts, tests/candidateAssurance.test.ts, tests/candidateWaveLineage.test.ts, tests/runRepairCandidateLifecycle.test.ts, tests/candidateRepairProtection.test.ts, tests/acceptanceOracle.test.ts. | S4 CONTRACT; S4/S6 UNIT; INTEGRATION; REAL_PROVIDER pending | 2026-09-24 S4 baseline retained; S6 consumption focused suite 11 files / 124 tests, full non-system 157 files passed / 1 skipped (931 passed / 7 skipped), typecheck/build passed; no REAL_PROVIDER claim. |
| Review and assurance recompilation | Impact and policy compile required reviewers, dimensions, and independent evidence. | S4 compiles exact configured read-only Reviewer identities for required dimensions and invokes those assignments even when ordinary review is disabled or the route is DIRECT. Each assignment emits candidate/impact/policy-bound evidence from the exact Reviewer session identity; failed or malformed output fails the report. S6 additionally requires the actual current participant ExecutionBinding/session and persisted result artifact before including Reviewer evidence. Agreement remains evidence only, not acceptance. Repair/rollback recompiles for the resulting current candidate. | DETERMINISTICALLY_VERIFIED (S4 deterministic gate; provider pending) | Live reviewer/provider behavior is not certified. S6 deterministic fixtures cover current session/provenance consumption; they do not certify live Reviewer behavior. | CandidateImpact; ParticipantPlan; frozen policy; S6 acceptance. | Keep assignments backed by actual independent current sessions; consume their evidence only through the AcceptanceOracle. | src/agents/reviewLifecycle.ts, src/core/run.ts, src/architecture/candidateAssurance.ts, src/architecture/acceptanceOracle.ts; tests/candidateAssurance.test.ts, tests/reviewLifecycle.test.ts, tests/reviewRepairCandidateLifecycle.test.ts, tests/runRepairCandidateLifecycle.test.ts, tests/acceptanceOracle.test.ts. | CONTRACT; UNIT; INTEGRATION; REAL_PROVIDER pending | 2026-09-24 S4 baseline retained; S6 consumption focused suite 11 files / 124 tests, full non-system 157 files passed / 1 skipped (931 passed / 7 skipped), typecheck/build passed; no REAL_PROVIDER claim. |
| Validation | Resolve required properties to approved validators; produce machine-verifiable current-candidate facts. | S4 deterministically maps impact dimensions to typed validation requirements, resolves them under frozen policy, and executes the approved project/configured action or fails closed when it is absent, disallowed, unsupported, or non-PASS. Checks bind candidate, impact digest, policy digest, requirement, and underlying check. S6 consumes resolved checks as evidence for their mapped assertions; validation PASS remains necessary evidence where required, never product acceptance by itself. | DETERMINISTICALLY_VERIFIED (S4 deterministic gate; provider pending) | Live provider, browser, and broader certification coverage remain pending; local validation and S4 PASS are not product acceptance. | WorkGraph; CandidateImpact; provider contracts; semantic stack profile; frozen policy. | Keep S4 requirements fail-closed and evidence candidate-bound; S6 requires the complete resolved evidence set and an accepted independent oracle disposition. | src/core/verify.ts, src/core/run.ts, src/architecture/validationRequirements.ts, src/architecture/candidateAssurance.ts, src/architecture/acceptanceOracle.ts, src/validators/external.ts; tests/validationProviders.test.ts, tests/candidateAssurance.test.ts, tests/candidateAssembler.test.ts, tests/runRepairCandidateLifecycle.test.ts, tests/acceptanceOracle.test.ts. | CONTRACT; UNIT; INTEGRATION; REAL_PROVIDER and BROWSER pending where required | 2026-09-24 S4 baseline retained; S6 consumption focused suite 11 files / 124 tests, full non-system 157 files passed / 1 skipped (931 passed / 7 skipped), typecheck/build passed; no live-provider claim. |
| AcceptanceOracle | Assertions, verification requirements, deterministic resolver, provenance evidence, independent acceptance oracle, required certification. | S6 still persists the current controller-evaluated ACCEPTED/REJECTED disposition bound to operation/candidate/policy/execution/controller epoch. S8 starts GitHub issue/branch/push/PR delivery only after current acceptance; objective completion recognizes accepted HANDOFF_ONLY when handoff is requested without push/PR finalization. Standalone `aeh sdd handoff` is explicitly unsupported and cannot trigger acceptance or delivery. | S6 deterministic gate accepted; S8 deterministic integration and CLI-surface remediation accepted by Campaign Director 2026-09-25. | No known deterministic acceptance-boundary gap. Live Lead/Reviewer/provider journeys and complete packed evidence remain pending. | Identity; impact; validation; review; human decisions. | Preserve current-candidate oracle and objective-completion gates without broadening S6 semantics. | src/architecture/acceptanceOracle.ts, src/architecture/objectiveCompletion.ts, src/core/run.ts, src/delivery/finalize.ts, src/delivery/handoff.ts; tests/acceptanceOracle.test.ts, tests/objectiveCompletionLifecycle.test.ts, tests/deliveryFinalize.test.ts, tests/cliS8Handoff.test.ts. | CONTRACT; UNIT; INTEGRATION; BUILD; SYSTEM_DETERMINISTIC; REAL_PROVIDER/BROWSER/OPA_PROVIDER/ADVERSARIAL/PACKED_E2E pending | S8 remediation evidence is recorded below. |
| Repair, recovery, and convergence | Typed diagnosis and bounded repair/replan; every repair creates a new candidate. | Production runTask preserves mechanically certain failure facts as DETERMINISTIC and sends ambiguous bounded failure evidence to the candidate-bound Semantic Assessor. Repair assembly carries post-assembly CandidateImpact into S4 assurance recompilation; quality repair/rollback revalidate and recompile for the current candidate. S6 consumes only assertions/evidence strength for that current candidate; prior-candidate acceptance evidence cannot support a repaired candidate. | PARTIAL | Broader recovery/quality convergence and live semantic-provider behavior remain unverified; S6 acceptance ordering and current-candidate checks are covered by deterministic fixtures. | Candidate truth; execution identity; review; semantic decision runtime; frozen policy. | Preserve deterministic retry/budget/state controls; route every repaired candidate through S4 recompilation and S6 acceptance before delivery. | src/agents/recovery.ts, src/core/run.ts, src/candidates/repair.ts, src/agents/reviewLifecycle.ts, src/architecture/candidateAssurance.ts, src/architecture/acceptanceOracle.ts; tests/recovery.test.ts, tests/candidateRepairProtection.test.ts, tests/candidateAssembler.test.ts, tests/candidateAssurance.test.ts, tests/runRepairCandidateLifecycle.test.ts, tests/reviewRepairCandidateLifecycle.test.ts. | CONTRACT; UNIT; INTEGRATION; SYSTEM_DETERMINISTIC; REAL_PROVIDER pending | 2026-09-24 S6-focused repair/review set within 11 files / 124 tests; full non-system 157 files passed / 1 skipped (931 passed / 7 skipped); scenario/concurrency system tests 2 files / 17 passed; REAL_PROVIDER pending. |
| Supply-chain provenance | Packed candidate receives verified SBOM, SLSA/in-toto provenance, BuildIdentity, and policy-required Cosign evidence. | Version-2 provenance remains bound to the exact CandidateRevision, configured packed artifact, BuildIdentity, policy and SBOM. Managed S8 handoff and finalization consume the existing policy-required supply-chain gate after current AcceptanceOracle acceptance and before public effects; standalone SDD handoff is explicitly unsupported. No S7 semantics changed. | S7 deterministic gate accepted 2026-09-24; S8 integration and CLI-surface remediation accepted by Campaign Director 2026-09-25. | REAL_PROVIDER signing/certification is not established; deterministic fixtures do not certify provider behavior. | S1 CandidateRevision and BuildIdentity; exact packed artifact; frozen supply-chain policy; S6 AcceptanceOracle-before-effects. | Keep required S7 provenance before public delivery effects; S13 retains provider certification. | src/provenance/generate.ts, src/delivery/handoff.ts, src/delivery/finalize.ts; tests/provenanceS7.test.ts, tests/deliveryFinalize.test.ts, tests/cliS8Handoff.test.ts. | CONTRACT; UNIT; INTEGRATION; BUILD; SYSTEM_DETERMINISTIC; REAL_PROVIDER pending | S8 remediation evidence is recorded below. |
| Delivery and reconciliation | Acceptance-gated exact effects with durable intent, receipt, and safe reconciliation. | S8 controller-owned flow persists intents, gates exact actions, and receipts execution or safe reconciliation. paseo.workspace.create is LOCAL_RESOURCE_CREATION, excluded from configured delivery effects, but remains S3-fenced and reconciled conservatively. The default issue path uses an internal controller workspace before acceptance and performs public issue/branch/push/PR effects only after current AcceptanceOracle acceptance and policy-required provenance. Uncertain execution is inspected before retry; unknown Paseo identity remains HUMAN_REQUIRED. The normal `sdd handoff` command returns explicit unsupported status before project loading and cannot enter this flow without a managed operation. | S8 deterministic gate accepted by Campaign Director 2026-09-25. External/provider lanes remain pending. | No known deterministic gap in the supported managed delivery flow. Standalone SDD handoff is intentionally unsupported; real GitHub/Paseo effects and complete packed provider reconciliation are not certified by fixtures. | Acceptance; authority; candidate identity; supply-chain policy. | S9 is the next eligible slice after S8 acceptance; S13 owns provider certification. | src/security/actionPolicy.ts, src/security/toolActionGate.ts, src/security/gatedAction.ts, src/security/actionReconciliation.ts, src/issues/workflow.ts, src/delivery/handoff.ts, src/delivery/finalize.ts, src/core/run.ts, src/cli.ts; tests/security/actionPolicy.test.ts, tests/security/gatedAction.test.ts, tests/security/toolActionGate.test.ts, tests/security/actionReconciliation.test.ts, tests/deliveryFinalize.test.ts, tests/cliS8Handoff.test.ts, tests/system/aehScenarioMatrix.test.ts, tests/system/aehConcurrencyCampaign.test.ts. | CONTRACT; UNIT; INTEGRATION; BUILD; SYSTEM_DETERMINISTIC; REAL_PROVIDER/PACKED_E2E pending | S8 remediation evidence is recorded below. |
| Runtime supervision | Controller owns process/provider leases, renewal, takeover, release, and cleanup. | Managed runtime mutations reload the shared snapshot under a root-scoped lock, validate it, and atomically persist service/provider state. The production Paseo adapter wraps operation-owned SDK materialization and participant/Lead turns with durable provider lease acquire, renewal, exact session inspection/stop, and release; current operation/candidate/policy/revision/token/epoch and participant or Lead-generation bindings are checked. Each lifecycle call has a unique owner ID, so overlapping writes cannot share one writer identity. Unsupported operation-owned SDK fallback fails closed. The lease's configured model-provider label is distinct from its Paseo-managed session inspection boundary. Expired leases remain fenced until observed quiescence or explicit reconciliation. Cancellation rechecks its current fence and releases provider leases only after exact-session quiescence. Control Center heartbeats its service record and drains owned service state on shutdown. R12 moves RUNNING/config/base-ref/portfolio bootstrap inside the existing catch, so strict base-ref errors terminalize as FAILED. | PARTIAL | Production adapter wiring, deterministic lease faults, and packed cancellation/takeover/restart-cleanup now have evidence. No REAL_PROVIDER startup/turn/renewal/cleanup receipt has been produced; provider certification remains a separate open lane. | Authority; accepted S5 session/context identity; supported Paseo provider. | Independently certify a supported Paseo lifecycle with actual provider startup, session identity, turn, renewal, quiescence and cancellation receipts; keep current operation/candidate/policy/revision/epoch fencing. | src/runtime/providerLifecycle.ts, src/runtime/supervisorV2.ts, src/runtime/managed.ts, src/paseo/runtimeCore.ts, src/paseo/runtimeInitialTurn.ts, src/operations/controller.ts, src/operations/state.ts; tests/paseoProviderLeaseLifecycle.test.ts, tests/paseoInitialTurnBarrier.test.ts, tests/runtimeSupervisorV2.test.ts, tests/security/controllerFencing.test.ts, tests/operations.test.ts, tests/operationLifecycleRegression.test.ts, tests/operationStateDurability.test.ts, tests/packed/s9ProviderLifecycleCampaign.mjs. | UNIT; INTEGRATION; BUILD; SYSTEM_DETERMINISTIC; PACKED_E2E (deterministic provider fixture); REAL_PROVIDER pending | 2026-09-25 S9 adapter, 13-file/137-test, concurrency, build, and packed lifecycle verification; R12 regression 1/1, typecheck, and fresh packed boundary inspection |
| Product, Control Center, and human interaction | Semantic projection, usable typed decisions, visible blockers, and safe control commands. | The authenticated loopback/CSRF server supplies the paired Human actor. Durable `OperationRecordV2` drives operation, candidate, stage/blocker, participant/error, counter, pause, and control projections; typed choices appear only for a current RUNNING/HUMAN_REQUIRED WAITING continuation whose request, continuation, frozen policy, candidate, execution revision, and epoch match. The App shows evidence and blocker detail, submits only operation/request/choice plus optional reason, refreshes after acceptance, and exposes server-derived pause/resume/cancel controls; the controller consumes exactly-once PAUSE/RESUME controls and applies pause only after a drain receipt proves writer quiescence. Server current-binding, expiry, replay, malformed, and mismatch checks remain authoritative. | PARTIAL | The mandatory real browser journey passes 46/46 assertions through the supported public start path and the real controller: current-scope review, UI choice submission, exactly-once consumption, continuation revalidation/resume, a second suspension, replay/stale rejection, pause/resume, scoped cancellation, and refreshed projections. The provider conversation was scripted for the fixture; packed human-interaction provider certification remains open. | Authority; runtime; operation state model. | Keep the current-binding checks and server-derived controls; provider-backed packed human interaction remains for the certification lane. | src/control-center/, src/security/humanDecision.ts, src/operations/state.ts, src/entry.ts, ui/control-center/src/api.ts, ui/control-center/src/App.tsx; tests/controlCenterOperationProjection.test.ts, tests/controlCenter.test.ts, tests/controlCenterApiContracts.test.ts, tests/humanDecision.test.ts, tests/operations.test.ts, tests/operationPauseResume.test.ts, tests/changeControllerOwnedDecision.test.ts, tests/browser/s9-control-center-journey.e2e.ts. | CONTRACT; UNIT; INTEGRATION; BUILD; BROWSER (46/46, scripted provider); PACKED human interaction pending | 2026-09-26 browser journey PASS; independent slice review pending |
| Observability and Engineering Evals | Candidate/execution-bound traces and metrics plus versioned reproducible advisory evals. | NDJSON and OTel spans exist; runner exists; no shipped eval corpus, metric instruments, or verified OTLP lane. | PARTIAL | No complete reproducible evidence/export path. | Execution identity; certification. | Ship corpus and verify local trace/metric export; keep evals advisory. | src/telemetry/, src/evals/; tests/metrics.test.ts, evals.test.ts. | UNIT; INTEGRATION; REAL_PROVIDER/OTLP lane pending | 2026-09-23 audit snapshot; not rerun |
| Security policy, isolation, sandbox, and SAST | OPA, role ceilings, tool gates, rootless isolation, network/scope controls, validator isolation, and candidate-bound SAST evidence. | S10 adds a deterministic rootless isolation provider: bwrap capability detection, minimal read-only host root, explicit toolchain binds, workspace-only writes, user/mount/PID/UTS/IPC namespaces, network denial by default, `--clearenv` plus environment allowlist, no-new-privileges, and explicit `ISOLATION_PROVIDER_UNAVAILABLE`/`ISOLATION_PROVIDER_UNSUPPORTED` blockers. Validator and external-tool commands run through it when `security.isolation.required`; the repository root is read-only except the declared writable paths (the workspace for configured validation commands and the evidence directory), as documented in SECURITY.md. Candidate-bound `SastEvidenceV1` binds adapter, tool version, command digest, raw-output digest, findings, workspace source digest, isolation evidence, and the exact CandidateRevision; stale, tampered, or missing evidence blocks, and the S4 security impact path requires it for opengrep/trivy. Real bwrap execution and a real local OSS Trivy v0.70.0 scan produced candidate-bound evidence for r1 (FAIL, secret finding) and r2 (PASS, clean). Podman/buildah are absent here and fail closed; OPA executable evaluation remains unavailable. | IMPLEMENTATION_COMPLETE — independent-review remediation applied; pending fresh re-review | No known deterministic S10 gate gap on the exercised rootless provider. Rootless Podman/OCI execution is unprovisioned in this environment and remains an explicit provider lane; OPA_PROVIDER and S13 provider certification remain pending. | S3 authority; S1 execution identity; local OSS isolation/SAST providers. | Independent slice review; keep provider absence fail-closed and SAST evidence candidate-bound. | src/security/isolation.ts, src/security/sastEvidence.ts, src/security/sandbox.ts, src/validators/external.ts, src/validators/toolCommand.ts, src/validators/commands.ts, src/core/run.ts, src/core/verify.ts, src/workers/podman.ts; tests/security/isolation.test.ts, tests/security/sastEvidence.test.ts, tests/security/validatorIsolationFailClosed.test.ts, tests/security/providerFailClosed.test.ts, tests/system/aehIsolationCampaign.test.ts, tests/packed/s10RealSastCampaign.mjs. | CONTRACT; UNIT; INTEGRATION; BUILD; SYSTEM_DETERMINISTIC; ADVERSARIAL; REAL_PROVIDER (local OSS bwrap + Trivy v0.70.0, not hosted); OPA_PROVIDER pending; PACKED_E2E/BROWSER not claimed | 2026-09-26 S10 real isolation and candidate-bound SAST campaign; canonical-invocation probe repair re-verified (`npm test -- tests/system/aehIsolationCampaign.test.ts` 3/3) |
| Contract and integration validation | Pact/OpenAPI/BDD and isolated service environments produce current candidate-bound reports and clean up. | Pact/OpenAPI and generic OCI integration environment exist; verifier may skip; lifecycle/coverage are incomplete. | PARTIAL | Required tool/provider absence and real lifecycle are not consistently blocking. | Validation requirements; S10 isolation/sandbox. | Prove contract diffs, service readiness/cleanup, and fail-closed missing tools. | src/providers/validation/; tests/openapi.test.ts, validationProviders.test.ts. | UNIT; INTEGRATION; PACKED_E2E pending | 2026-09-23 audit snapshot; not rerun |
| Browser and visual validation | Browser/visual evidence is candidate-bound and traceable to packed UI behavior. | External validator adapter remains available. S4 maps UI/browser impact to a typed `browser-test` requirement and compiles a corresponding independent Reviewer assignment; a configured Playwright action runs through its specific adapter or blocks. No live browser provider/candidate journey is certified. | PARTIAL | Browser evidence and Control Center journeys are not produced end-to-end. | Validation resolver; CandidateImpact; Control Center; packed candidate. | Run required browser assertions and persist candidate-bound trace/screenshot. | src/validators/external.ts, src/architecture/candidateAssurance.ts, src/core/run.ts, toolEvidence.ts; tests/candidateAssurance.test.ts, provider contract tests. | CONTRACT; UNIT; BROWSER pending; PACKED_E2E pending | 2026-09-24 S4 deterministic mapping tests; browser provider journey remains pending |
| Real-provider certification matrix | Deterministic and real-provider lanes remain distinct; claimed capabilities have complete fresh packed-candidate evidence. | CertificationCore, 22-capability matrix, packed bootstrap, and Codex adapter exist. S7 verifies deterministic candidate/build/artifact/policy/builder/SBOM/signature bindings; its fake Cosign boundary is expressly fixture evidence and does not promote CertificationCore or the matrix. Provider matrix/journeys remain incomplete. | PARTIAL | No full real-provider/Paseo/Podman/browser campaign; no provider-backed signing certification is available from S7 fixtures. | All claimed capability paths; acceptance and evidence gates; S13 real-provider certification. | Certify each claimed provider with receipts and independent oracle in S13; preserve S7 deterministic/REAL_PROVIDER lane separation. | src/certification/; src/provenance/generate.ts; tests/certification.test.ts, tests/certificationMatrix.test.ts, tests/provenanceS7.test.ts, tests/projectSchemaS7.test.ts. | CONTRACT; UNIT; REAL_PROVIDER pending; PACKED_E2E incomplete | 2026-09-24 S7 deterministic fixtures passed the provenance gate; the `cosign` CLI is unavailable, no provider signing lane executed, and no provider certification is claimed. |
| Learning and feedback | Versioned post-run candidates; independent evaluation and governed adoption; no implicit authority mutation. | Memory recall, telemetry, evals, and within-run repair exist; no cross-run learning pipeline. | DEFERRED | Learning and persistent skill promotion are explicitly post-v2. | Observability; human-governed policy. | Reopen only with versioned candidate and explicit adoption gate. | src/memory/, src/evals/; advisory use only. | UNIT for advisory contracts; adoption lane deferred | 2026-09-23 audit snapshot; not rerun |
| Self-hosting readiness | Deterministic composite gate plus disposable self-modification fixture. | Packed bootstrap and some system campaigns exist; no complete composite or fixture proof. | BLOCKED | Identity, acceptance, recovery, provider, adversarial, and isolation evidence remain incomplete. | Every required certified row, especially security, delivery, and certification. | Pass the composite gate and prove the source checkout remains untouched through a disposable fixture campaign. | src/certification/bootstrap.ts; current conformance/ledger evidence is insufficient. | PACKED_E2E incomplete; REAL_PROVIDER pending; ADVERSARIAL pending | 2026-09-23 audit snapshot; not rerun |

## Approved dependency DAG

The DAG reflects the reconciled S1-S14 ownership. Existing candidate truth and controller-fencing foundations are prerequisites. S2, S3, and S5 can proceed in parallel after S1 interfaces are frozen. S7 may progress alongside S4-S6; S9 may proceed alongside S6-S8 after authority and context identity contracts are fixed. S10 is the security isolation slice. S11 adapters may be developed earlier, but their certification evidence waits for stable acceptance and policy interfaces.

~~~mermaid
flowchart TD
  F[Existing candidate truth and controller-fencing foundation] --> S1[S1 Frozen Execution and Skill Identity]
  S1 --> S2[S2 Production Semantic Decision Runtime]
  S1 --> S3[S3 Authority Kernel and Human Control]
  S1 --> S5[S5 Progressive Context and Paseo Session Identity]
  S2 --> S4[S4 CandidateImpact and Assurance Recompilation]
  S3 --> S4
  S5 --> S4
  S3 --> S6[S6 AcceptanceOracle and Objective Completion]
  S4 --> S6
  S1 --> S7[S7 Supply-Chain Provenance Gate]
  S6 --> S8[S8 Controller-Owned Delivery and Reconciliation]
  S7 --> S8
  S3 --> S8
  S3 --> S9[S9 Runtime Supervision and Control Center]
  S5 --> S9
  S3 --> S10[S10 Security Isolation, Sandbox and SAST]
  S1 --> S10
  S6 --> S11[S11 Contract, Integration, Browser and Visual Validation]
  S9 --> S11
  S10 --> S11
  S1 --> S12[S12 Observability and Engineering Evals]
  S6 --> S13[S13 Real-Provider Certification Matrix]
  S7 --> S13
  S8 --> S13
  S9 --> S13
  S10 --> S13
  S11 --> S13
  S12 --> S13
  S13 --> S14[S14 Self-Hosting Composite Gate]
~~~

The cross-slice dependency summary is:

- S1 → S2, S3, S5; it also establishes identity inputs for S7, S10, and S12.
- S2 + S3 + S5 → S4.
- S3 + S4 → S6.
- S7 can progress alongside S4-S6 after candidate/build identity is available; S6 + S7 + S3 → S8.
- S3 + S5 → S9, which can proceed alongside S6-S8.
- S3 + S1 → S10; S6 + S9 + S10 → S11.
- S12 proceeds in parallel after S1.
- S13 requires the implemented and gated paths for each capability being claimed, including relevant S6-S12 evidence.
- S14 requires all required capability certifications and the disposable self-modification campaign.

## S1-S14 implementation slices

### S1 — Frozen Execution & Skill Identity

**Objective:** establish truthful, immutable policy, role, skill, blueprint, binding, context/prompt, and structured-result identity at launch and result boundaries.

**Dependencies:** existing candidate truth and controller-fencing foundation.

**Definition of Done:**

1. A deterministic compiler produces immutable ResolvedOperationPolicy and per-role RoleInvocationPolicy; model output cannot choose, weaken, or widen either.
2. A versioned ExecutionBlueprint binds project, operation execution revision, candidate revision/digest, controller epoch, policy, WorkGraph/ParticipantPlan/catalog, participant role/specialization, ToolPack, resource claims, validation resolution, output contract, and SkillManifest digests.
3. Per-participant ExecutionBinding binds generation, approved runtime/model/session, actual ContextManifest and PromptManifest digests, and applicable controller-issued lease identity.
4. Accepted ephemeral procedure content is retained in SkillManifest, projected verbatim only to its assigned authorized participant, and included in the prompt/binding digest. An ID-only assignment fails.
5. Every production structured-result launch path propagates the full versioned binding. The gateway rejects missing, stale, replayed, wrong-candidate, wrong-generation, wrong-epoch, wrong-policy, wrong-blueprint, wrong-skill/context/prompt, wrong-output-contract, and wrong-runtime results.
6. Candidate assembly invalidates all execution bindings and results for the previous candidate. Controller takeover invalidates old-epoch writers and leases.
7. Obsolete identity versions fail with explicit unsupported-version/migration errors; no dual-read compatibility path is added.
8. Deterministic tests cover valid propagation and each rejection case across direct, Podman, Paseo, and distributed transports. These tests do not claim real-provider certification.

recordRevision remains the append-only event/order revision. operationExecutionRevision changes only when execution semantics change, so routine stage events do not stale active bindings.

**Gate:** all eight Definition of Done items hold across production launch/result paths; focused deterministic evidence is complete. No real-provider certification is implied.

### S2 — Production Semantic Decision Runtime

**Objective:** wire bounded semantic assessment into public intent, route, stack, issue, failure, and impact paths, with typed evidence and deterministic post-checks.

**Dependencies:** S1 identity interfaces and the approved model/provider interface.

**Gate:** each public semantic path has an explicit DETERMINISTIC, MODEL, or HYBRID mechanism, preserves unknowns and provenance, and cannot grant authority or bypass policy.

### S3 — Authority Kernel & Human Control

**Objective:** consume scoped HumanDecisions; enforce controller/current-epoch ownership and cancellation fencing; correct OPA canonical-role matching; and make action and policy authority semantics consistent across mutations and effects.

**Dependencies:** S1 execution identity and existing candidate truth.

**Gate:** each decision, mutation, cancellation, and sensitive action is checked against current operation, candidate, policy, actor, scope, lease, and epoch; product choice remains distinct from action authorization.

**Ownership boundary:** security-policy facts required for role ceilings and action authority belong here. Rootless sandbox, network isolation, validator isolation, provider sandbox certification, and SAST closure belong to S10.

### S4 — CandidateImpact & Assurance Recompilation

**Objective:** bind actual post-assembly impact to the current candidate and recompile assurance, reviewers, validation, acceptance assertions, and evidence strength from that impact and policy.

**Dependencies:** S2 semantic runtime, S3 authority/policy, S5 context/session identity, and candidate truth.

**Gate:** no impact result can weaken a policy floor; required dimensions produce required independent evidence; missing or unresolved high-risk impact blocks acceptance.

### S5 — Progressive Context & Paseo Session Identity

**Objective:** wire full progressive Context Runtime v2, JIT/addressable references, continuation semantics, retrieval lifecycle, broader progressive projection, and create/reuse/rotation of PaseoSessionBinding.

**Dependencies:** S1 execution identity and a frozen controller-issued context-authorization interface. S5 can proceed alongside S3 after the shared interface is fixed; S3 owns authority semantics.

**Gate:** each launch/result binds the actual authorized context and prompt; continuation and retrieval remain candidate/session-bound; any session binding mismatch rotates or rejects reuse.

### S6 — AcceptanceOracle & Objective Completion

**Objective:** complete AcceptanceAssertion, VerificationRequirement, OracleResolver, EvidenceBundle, and AcceptanceOracle disposition; enforce objective Definition of Done and managed Lead acceptance ordering before delivery.

**Dependencies:** S3 authority/human controls and S4 impact-driven assurance.

**Gate:** every required assertion has sufficient current candidate-bound evidence; validation PASS or reviewer opinion alone cannot accept; completion evaluates the complete objective evidence set.

**Campaign status:** S6 and S7 were accepted on their deterministic gates, 2026-09-24. S8 was independently accepted by the Campaign Director on its deterministic gate on 2026-09-25.

### S7 — Supply-Chain Provenance Gate

**Objective:** bind packed-candidate SBOM, provenance, BuildIdentity, and policy-required signing evidence to the artifact and make required provenance a deterministic gate.

**Dependencies:** candidate/build identity. This slice may progress alongside S4-S6.

**Gate:** missing, tampered, stale, or wrong-builder evidence blocks the delivery path wherever policy requires it. Deterministic fixtures remain distinct from provider-backed signing evidence.

**Campaign status:** the deterministic S7 gate, including the schema-guard remediation, was accepted by the Campaign Director on 2026-09-24. S8 was independently accepted by the Campaign Director on its deterministic gate on 2026-09-25.

### S8 — Controller-Owned Delivery & Reconciliation

**Objective:** route every requested external effect through controller-owned ActionIntent, ToolActionGate, ActionReceipt, and safe reconciliation.

**Dependencies:** S3 authority, S6 acceptance, and S7 provenance where required.

**Gate:** no effect occurs before current acceptance and required policy gates; uncertain non-idempotent outcomes are inspected and reconciled before retry or completion.

**Campaign status:** **SLICE_ACCEPTED** by the Campaign Director on 2026-09-25 after independent diff review and deterministic reruns. The standalone `aeh sdd handoff` command is explicitly unsupported because no managed accepted-delivery lifecycle is available to a normal CLI invocation. S9 had not started at the time of acceptance.

**S8 remediation evidence — 2026-09-25:** The final focused S8 set passed 14 files / 198 tests. `npx tsc --noEmit` and `npm run build` exited 0 (release `release-1790322187860-3232428-0507f39b`). Full non-system Vitest passed 162 files with 1 skipped (1,014 passed, 7 skipped); affected deterministic scenario/concurrency suites passed 2 files / 17 tests. The new CLI regression passed 2 tests and proves unsupported handling occurs before project-config loading and produces no workspace/public effects. The Flash worker's baseline run failed both tests against the stale command and passed both against the remediation. The Campaign Director independently reran the focused S8 set (7 files / 89 tests), TypeScript, build, full non-system suite, and affected system suites; all passed. `git diff --check` and the normative-target equality check passed. Fixtures are deterministic evidence; REAL_PROVIDER, BROWSER, OPA_PROVIDER, ADVERSARIAL, and PACKED_E2E claims remain pending. **Campaign Director disposition: SLICE_ACCEPTED.**

### S9 — Runtime Supervision & Control Center

**Objective:** complete controller-owned process/provider lifecycle, leases, renewal/release, takeover, drain/cleanup, semantic operation and participant state projections, and usable typed Control Center decisions/controls.

**Dependencies:** S3 authority and S5 session/context identity. This slice may proceed alongside S6-S8.

**Gate:** single-writer ownership, current-epoch fencing, safe cancellation/resume, accurate projections, and scoped decisions are evidenced end to end.

**Campaign status:** **IMPLEMENTATION_COMPLETE — pending independent slice review.** The mandatory real browser journey passed against the current candidate (1 Playwright test, 46/46 assertions) through the supported public start path, a real Playwright Chromium, the real Control Center UI/server, and the real controller: pairing, rendered current-scope review, UI decision submission, exactly-once controller consumption, continuation revalidation/resume, replay/stale rejection, pause with durable `PAUSED` after drain/fencing, resume, scoped cancellation with writer fencing, and refreshed API/rendered projection equality. The external model conversation was replaced by the file-scripted deterministic Paseo boundary (`AEH_DETERMINISTIC_PASEO_RUNTIME=1`); that is fixture evidence, not REAL_PROVIDER certification. The implementing lead does not accept its own slice; a fresh independent reviewer owns slice acceptance. S10 remains locked.

**R12 remediation evidence — 2026-09-25:** the packed Lead runtime's selected release had config/base-ref/portfolio bootstrap outside the controller's `try`, so strict `baseRef=main` failure in a non-Git disposable root exited after persisting `RUNNING` and left a dead-owner operation nonterminal. The repair in `src/operations/controller.ts` places that bootstrap inside the existing fenced failure-finalization boundary; strict base-ref failure still terminalizes as `FAILED`. The delegated Flash test reproduced the old code: `npx vitest run tests/operationStartupFailure.test.ts` in isolated baseline `c036471` exited 1 with 1 test failed at `resolveBaseRef` before the catch. After selective integration, `./node_modules/.bin/vitest run tests/operationStartupFailure.test.ts --reporter=dot` passed 1 file / 1 test and `npm run typecheck` exited 0. The Campaign Director independently ran the earlier test draft — 1 file / 1 test passed; typecheck exited 0; build exited 0 (`release-1790359341925-17-d65e1b0f`). The fresh packed R12 artifact is `/tmp/aeh-s9-r12-pack/agentic-engineering-harness-0.8.4.tgz`, SHA-256 `77a0c39f580053b67abe2ee444e6b4c2995ecdc417a66613eb56d5dc20a1782d`, selecting `release-1790359422074-34-41c17389`; packed inspection confirms the bootstrap is inside the catch. The Director owns initialization/recovery of the same disposable operation; no S9 browser decision or cancellation has been submitted by the implementation lead.

**Browser journey evidence — 2026-09-26:** the maintained Playwright harness in `tests/browser/` now starts a real governed CHANGE operation through the public `npm run aeh -- operation start change` path with the file-scripted deterministic Paseo runtime, so the real controller owns the Spec Manager suspension, exactly-once consumption, and resume. The journey passed 46/46 assertions against candidate release `release-1790386441389-339964-cb4c88d0` (gitSha `c036471`, dirty, package 0.8.4): pairing, rendered current scope (operation, candidate digest, policy digest, `operationExecutionRevision`, controller epoch), UI decision submission, exactly-once consumption with replay/unknown/out-of-scope/foreign/CSRF rejection, continuation revalidation and resume, a second controller-owned suspension, pause to durable `PAUSED` with an empty drain receipt and stale-writer fencing, resume back to `HUMAN_REQUIRED`, scoped cancellation to `CANCELLED`, and final API/rendered projection equality. Sanitized evidence: `docs/evidence/s9/s9-playwright-journey.json`, `s9-playwright-paired-home.png`, `s9-playwright-final-projection.png`. This is BROWSER plus deterministic-provider fixture evidence, not REAL_PROVIDER certification.

### S10 — Security Isolation, Sandbox & SAST

**Objective:** prove rootless sandbox and network/scope isolation, validator-command isolation, and candidate-bound SAST evidence under the authority policy.

**Dependencies:** S1 execution identity and S3 role/action policy.

**Gate:** prohibited actions and out-of-scope access are denied; sandbox/provider evidence and SAST reports are candidate-bound. This provider/isolation certification belongs to S10 even when policy semantics are prerequisites in S3.

**Campaign status:** **IMPLEMENTATION_COMPLETE — pending independent slice review.** The S10 WorkGraph, frozen provider/evidence contracts, delegation assessment, and exact environment facts are in [S10 WorkGraph](CORE_ARCHITECTURE_V2_S10_WORKGRAPH.md). The pre-S10 gaps were independently reproduced by a bounded worker in an isolated Paseo worktree: [baseline gaps](evidence/s10/baseline-gaps.md).

**Environment facts:** kernel `6.18.52-1-cachyos-lts`, uid 1000, `kernel.unprivileged_userns_clone = 1`; `bwrap` (bubblewrap) `0.13.0` available; `runc`, `unshare`, `newuidmap`, `newgidmap` available; `podman`, `buildah`, `crun`, `slirp4netns`, `fuse-overlayfs`, `opa`, `opengrep`, `semgrep` absent; `sudo` unavailable (password required), so no system package installation. Trivy `v0.70.0` (Apache-2.0) was provisioned as a user-local OSS binary with the published release checksum (`trivy_0.70.0_Linux-64bit.tar.gz` SHA-256 `8b4376d5d6befe5c24d503f10ff136d9e0c49f9127a4279fd110b727929a5aa9`; extracted binary SHA-256 `379d59f24a4a828c55de5f0b91b6805cc35d13580180b658820e648611256166`) for real SAST execution only; it is not a committed dependency.

**Real rootless isolation evidence — 2026-09-25:** `tests/system/aehIsolationCampaign.test.ts` executes the actual `bwrap` provider (3 tests passed) and proves, from inside the sandbox: the host uid is preserved without privilege (`uid=1000`), `CapEff=0000000000000000` and `NoNewPrivs=1`; only `lo` exists in the new network namespace and a real TCP connect to `1.1.1.1:80` fails with `ENETUNREACH`; a live host process PID is not visible under `/proc`; host `$HOME/.ssh` and sibling host directories are masked; `/etc` is read-only; writes to the declared workspace land on the host while writes outside it do not; an inherited host environment secret is absent. The validator-isolation case runs a required opengrep-adapter command inside the sandbox: repository mutation is denied, egress is denied, the check passes on valid structured output, and the check records full isolation evidence. The focused deterministic contract suites `tests/security/isolation.test.ts`, `tests/security/validatorIsolationFailClosed.test.ts`, and `tests/security/providerFailClosed.test.ts` cover argument construction, unsupported providers, environment rejection, and explicit `ISOLATION_PROVIDER_UNAVAILABLE`/`ISOLATION_PROVIDER_UNSUPPORTED` blockers; an unavailable provider never runs the validator and never reports a silent SKIP or PASS.

**Candidate-bound SAST evidence — 2026-09-25:** `S10_TRIVY_BIN=/tmp/opencode/s10-tools/trivy/0.70.0/trivy node tests/packed/s10RealSastCampaign.mjs "$(pwd)"` ran against built release `release-1790389747122-486790-a8c37684` and passed all assertions. Candidate r1 (planted GitHub PAT) produced a real FAIL with one `github-pat` finding and candidate-bound `SastEvidenceV1`; candidate r2 (secret removed) produced a clean PASS with zero findings. Both artifacts are persisted under the candidate-scoped directory `.harness/evidence/sast/<candidateId>-r<revision>-<identityDigest12>/`, bind the exact candidate/workspace source digest, tool version, command digest, raw-output digest, findings, and isolation evidence, and verify by digest. The r1 evidence is rejected as `SAST_EVIDENCE_STALE` for r2, proving exact-revision traceability. Deterministic contract tests (`tests/security/sastEvidence.test.ts`) cover persist/load/verify, stale/tampered/missing rejection, workspace drift refusal, and fail-closed missing-tool behavior. Sanitized summary: [real SAST campaign](evidence/s10/real-sast-campaign.json). This is REAL_PROVIDER evidence for a local OSS scanner plus a real rootless provider; it does not certify hosted providers, rootless Podman/OCI execution, or S13 provider certification.

**Deterministic/build evidence — 2026-09-25:** `npm run typecheck` exited 0; `npm run build` exited 0 (`release-1790389747122-486790-a8c37684`). The full non-system suite passed 175 files with 1 skipped (1,134 tests passed, 7 skipped) except `tests/paseoSdkResolve.test.ts` (3 failures); that file was rerun in a clean detached worktree at baseline `0afebcd` and failed 3/3 there as well, confirming environment drift from a mise-installed Paseo CLI rather than an S10 regression. The relevant system lanes passed: scenario matrix, concurrency campaign, and isolation campaign — 3 files / 20 tests. The adversarial suite passed 26 tests with its two long-recorded S6 objective-completion fixture failures unchanged. Rootless Podman/OCI execution, OPA executable evaluation, BROWSER, and complete PACKED_E2E certification remain separate open lanes owned by later slices.

**Independent-review remediation — 2026-09-26:** the fresh independent reviewer rejected the first S10 evidence because the isolation campaign's host-home probe tested `${os.homedir()}/Desarrollo`, which npm/npx turns into a bwrap bind-destination parent; the campaign therefore failed under `npm test`/`npx vitest` while passing with a direct binary invocation. The probe now asserts a host-only home sentinel that is independent of bind-destination parents, and the security row documents the configured-validator workspace exception consistently with SECURITY.md. Re-verification: `npm test -- tests/system/aehIsolationCampaign.test.ts` 1 file / 3 tests passed; `npx vitest run tests/system/aehIsolationCampaign.test.ts` 1 file / 3 tests passed; focused S10 set 5 files / 30 tests passed; typecheck exit 0; `git diff --check` clean. S10 remains pending a fresh independent re-review.

### S11 — Contract / Integration / Browser / Visual Validation

**Objective:** produce candidate-bound Pact/OpenAPI/BDD, isolated-service integration, browser, and visual evidence, with required-tool absence failing closed and environments cleaning up.

**Dependencies:** S6 acceptance/evidence contracts, S9 Control Center/runtime, and S10 isolation. Adapters may be developed earlier; certification waits for stable interfaces.

**Gate:** required contract and UI assertions resolve to approved providers and current-candidate evidence; missing providers block; browser and visual journeys are reproducible and traceable.

### S12 — Observability & Engineering Evals

**Objective:** complete candidate/execution-bound traces and metrics plus a versioned, reproducible advisory eval corpus and export path.

**Dependencies:** S1 execution identity. This slice may progress in parallel with S2-S11.

**Gate:** telemetry identity is verifiable, local export is reproducible, and evals remain advisory rather than policy or authority.

### S13 — Real-Provider Certification Matrix

**Objective:** certify each claimed capability through freshly packed candidates, real provider startup/execution receipts, deterministic oracle checks, and required independent review.

**Dependencies:** each claimed capability's implementation path and relevant S6-S12 evidence.

**Gate:** each claimed row has fresh candidate/build-bound REAL_PROVIDER evidence; deterministic fixtures never substitute for provider execution; BROWSER and ADVERSARIAL lanes remain separately reported.

### S14 — Self-Hosting Composite Gate

**Objective:** aggregate required versioned certification evidence and prove a disposable self-modification campaign leaves the source checkout untouched.

**Dependencies:** all required prior slice gates, especially S10-S13.

**Gate:** deterministic composite gate passes; packed candidate mutations and effects remain inside the disposable fixture; reconciliation and cleanup succeed; no model declares readiness.
