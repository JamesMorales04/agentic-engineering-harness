# Eval Corpus

Store repeatable engineering tasks here. Avoid secrets or proprietary source snapshots in the harness repository itself. Consumer projects may maintain private eval corpora that use the same format.
